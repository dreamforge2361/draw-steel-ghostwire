# Draw Steel - Ghostwire Build

Foundry VTT **module** for Ghostwire. It runs on the stock **Draw Steel** system (`draw-steel`) and does **not** fork or replace that system.

## Architecture

| Layer | Package |
| --- | --- |
| System (unchanged) | `draw-steel` |
| Ghostwire world | Your Foundry world bound to Draw Steel |
| This module | Reskin / CSS, localization, Ghostwire compendium packs, optional sheet overlays |

Class design rules live in Ghostwire Development Master docs outside this repo. This repo ships Foundry packaging only.

## Target stack

- Foundry VTT **14** (minimum / verified **14.367**)
- Draw Steel **1.1.2+**

## Characteristic lang map (0.1.1+)

Ghostwire display labels override Draw Steel characteristic **full** names. Abbreviations (M / A / R / I / P) stay Draw Steel so potency text keeps working.

| Draw Steel | Ghostwire label |
| --- | --- |
| Might | Physique |
| Agility | Reflex |
| Reason | Logic |
| Intuition | Instinct |
| Presence | Persona |

Also: Wealth → **Nuyen**; sheet chrome labels say Ghostwire instead of Draw Steel.

Style tokens live in `styles/ghostwire.css` (`--ghostwire-*`). The module adds `ghostwire` / `ghostwire-theme` classes on `document.body` at init.

## Install (dev)

1. Install Draw Steel from Foundry’s system browser.
2. Clone or symlink this repo into your Foundry Data folder:

```text
Data/modules/draw-steel-ghostwire
```

3. Enable **Draw Steel - Ghostwire Build** in the world module list.
4. Reload the world.

## Install (release)

Use Foundry **Install Module** / update from:

`https://github.com/dreamforge2361/draw-steel-ghostwire/releases/latest/download/module.json`

## Status

- `0.3.127` — **Grenades you place before you roll, a cycle that stops at the door, pregens that can catch their breath, hit FX that need nothing, and bodies worth searching.** **The Throw rewrite is the headline.** 0.3.126's grenade auto-centred on whatever token was targeted, put the circle down *after* the power roll, used a fail / success / critical-success table, and left the drawing on the map afterwards. All four are gone. The blast is **15 ft**, not 20; **you place it yourself**, before any dice, anywhere within **60 ft** of your token, with the blast and the reach both drawn live under the cursor and a click past the reach **refused with the distance** rather than dragged back; and the circle is **deleted the moment the throw resolves**, including on an abort after the place — leftover circles were the concrete thing that made tokens hard to pick up at the table. The order matters as much as the numbers: pick → place → roll → saves → card → delete, so a player who changes their mind about the corner has not already spent a roll on the log. The save table is Draw Steel's tier ladder read straight — **Low** takes the thrower's tier in full *and* the rider, **Mid** takes **half damage rounded down and no rider at all**, **High** takes nothing — and the old "a critical success still eats the middle impact" row is retired, because a natural 19-20 on the save is already tier 3, which is already High. Riders are **Low only**: Flash-Bang's Dazed and the EMP's chrome suppression do not happen on a Mid save however well the thrower rolled, and the EMP's **2-round** duration is still the thrower's HIGH result, still only against a Low save. The chat card now prints the save total, the rung, and *why* — `4 damage (half, rounded down)` reads differently from `4 damage`. One thing found on the way: **Foundry 14 merged MeasuredTemplate into Region**, so 0.3.126's `createEmbeddedDocuments("MeasuredTemplate")` had nothing to create into on 14.367 at all; the circle is a **Region** now, which is the v14 spelling of the same object, and it highlights and deletes. **The Wire State cycle stops offering the seat.** `Disconnected → Linked → Overlay → Disconnected`, three rungs, and **Jumped In is not one of them** — one extra press of a macro should not stamp meat-inert on a Rigger's body and move their guns. Jump-In did not go anywhere: it is still a first-class choice in the Wire state picker and still the landing state of `jumpIn()`. It is simply not somewhere you arrive by pressing the same key three times. **Every pregen finally has their default abilities.** Draw Steel grants Catch Breath, Escape Grab, Grab, Knockback, Stand Up, Advance and Disengage through `hero.defaultItems`, which Foundry applies when it *creates* an Actor — and the nine dossier heroes are built by a Node script, so for nine builds they got none of them. A player handed Vira could not spend a Recovery from her own sheet. All seven are embedded now, along with Ghostwire's five swaps (**Spot Target, Rush, Take Cover, Patch Up, Drive**), from a committed snapshot so a regen still reproduces byte for byte with no Foundry installed. The two generic **Free Strikes** (B44c) and the **Matrix Verbs** (B117) stay off, as they always have. **Ghostwire draws its own hit FX.** A gun hit, a melee hit and a grenade going off now land with a sound and a flash, and **no other module is required for either half** — the sounds are module assets and the flash is drawn with PIXI. Sequencer and JB2A, when they are installed and switched on, replace the flash with their video effects; Automated Animations is never involved. It sits *beside* B40 rather than on top of it: B40 fires on ability **use**, this fires on ability **result**, so a burst is the muzzle and then the impact, and turning either off leaves the other working. **And bodies are worth searching.** **51** bestiary NPCs across ARG and Ironclad security, the Reach streets, the rivals, Lazarus, Aequitas, the magical societies, the Veil and the two Chrome Raiders now carry **161** embedded, droppable, liquidatable Items — the gun their stat block actually fires, ammunition in the load it actually uses (gel on Mandate and Lazarus sidearms, AP on Ironclad, loose shells on a ganger), and the trade kit that fits the role. **32 carry nothing, and that is the other half of the lock**: beasts have no pockets, ICE has no body, the risen rose without their kit, and a nightclub patron with a lockpick set is exactly the filler this forbids. Packs rebuilt with Foundry closed. Smokes: `node tools/wave-03127-smoke.mjs` (the new one), `node tools/wire-state-toggle-smoke.mjs`, `node tools/pregen-regen-smoke.mjs`, `node tools/wave-03126-smoke.mjs`, `node tools/kiosk-smoke.mjs`. Foundry checklist `docs/directors/03127-smoke.md`.
- `0.3.126` — **Emergency Reagents, guns that run dry, and grenades that are areas.** **Raw Reagent** is the restock a Medic buys when downtime is not on offer: a ¥2,000 ampoule, a **maneuver**, **+1 Reagent**, and that is all. It runs on the same `consumableUse` seam the street chems and the two medical kits already use, with one new kind of effect — a *class resource* rather than Stamina, Recoveries or a condition — which needed exactly two things this seam did not have. The grant goes through `planReagentGrant` and `capacityOf` from 0.3.125, so a Medic at 9 of 10 gains **1** and the card says so, and there is deliberately **no second dose currency**: Reagents live in `system.hero.primary.value` and nowhere else. And the refusals land **before the card posts** — a non-Medic is holding precursor with nothing to crack it into, a Medic at capacity would be paying ¥2,000 for zero, and neither spends the ampoule to find that out. The **3-Pack** is ¥5,500 for one inventory row of **quantity 3** carrying the single's use block byte-for-byte, because a container document would have been a second ability and a second SKU to save one drag. **Guns spend rounds now.** There are no magazine Items: loaded state is `flags.<module>.ammo = { count, type }` **on the gun you are holding**, ammunition is a stack of rounds in your pocket, and **Reload** is a maneuver that moves the second into the first — **topping to capacity in one action**, not a round at a time. Capacity is the gun's family, stamped on all **27** SKUs as `gear.ammoFamily` and mirrored by a table the smoke re-derives from the folder tree on every run: **Handgun 12 · SMG 30 · Longarm 20 · Shotgun 8 · Heavy 50**. Firing debits one and puts **which rounds went downrange** on the attack card; an empty magazine refuses with **“You are out of ammunition.”** and never reaches the roll dialog. Switching type hands whatever is still in the gun **back to the pocket as the old type** — and a switch you cannot fill is refused rather than silently unloading you. The three ammunition SKUs therefore ship as a **box of 30 rounds** at their printed price, because that is what makes “spend from the stack” and “leftovers go back to the stack” both literal. **Stick-n-Shock** is the specialty round it always claimed to be: on a hit, **1 Stamina and Dazed**, *instead of* the gun's printed damage — a rifle that still cracks ribs for 8 and then stuns is not non-lethal ammo. A gun on a §5F hardpoint spends nothing: 0.3.112 Mounted fire is untouched. **And Blast grenades stop being free strikes.** B49 could only make a single-target strike out of a weapon treasure, which is the wrong shape for a thing that goes off in a room. One shared **Throw** main action now picks a grenade, places a **20 ft circle**, and rolls a **Reflex** save for every token under it — **fail** takes the thrower's power-roll tier in full, a **normal success** takes nothing, and a **critical success still eats the middle impact**, which is the lock and not a typo: a crit is getting most of the way clear, not a clean shrug. Throw on a **HIGH** result — tier 3, or a natural 19-20 — and a failed save takes the grenade's high rider on top. The thrower's tier is read off the thrower's own card (F11's `messageCritical`), so every edge, bane and critical the system already applied is the one the saves are read against. **Flash-Bang's impact is Dazed** and its misleading 4 electrical is stripped; the new **EMP Grenade** (¥5,000) damages nothing and **suppresses every implant on a failed save — 1 round, or 2 on a HIGH throw** — through F12's existing chrome ladder, grade shifts and all. Thermite and Shaped Charge keep their strikes: they are placed against a wall at Adjacent, not lobbed. **The Ammo Counter stopped double-selling broken copies** — the inert **Frag Grenade** and **Flash-Bang** duplicates are deleted (the thrown originals are the usable ones) and **Smoke**, which had no thrown twin, *became* one: same `_id`, same `_dsid`, now a Weapons Cage row that puts a circle down with **no save** and Cover/Conceals everything inside it. Packs rebuilt with **Foundry closed**; pregens regenerated for the ammunition stamp. Smokes: `node tools/wave-03126-smoke.mjs` (the new one), `node tools/g3-lang-style-tokens-smoke.mjs`, `node tools/g4-skill-on-weapon-smoke.mjs`, `node tools/kiosk-smoke.mjs`, `node tools/pregen-regen-smoke.mjs`. Foundry checklist `docs/directors/03126-smoke.md`.
- `0.3.125` — **Disengage gets written down, the two medical kits work, and Reagents stop being a battery.** **Disengage was never in the book.** VOIDMARK answering “not on this channel” was correct: `docs/raw/04-combat.md` said the move action *is* spending Speed, and every Kit row and chrome rider printing “+N Disengage” pointed at a procedure that did not exist. It does now, in Ghostwire's own voice — the move action is **Move or Disengage**, one of the two; Disengage **shifts your Disengage value** and does not invite opportunity pressure for leaving reach, but it is still a shift, so walls and hazards are unchanged; and the value is **baseline 1 + Kit bonus + chrome and feature riders**, which is exactly what Reaction Enhancer (+1) and Wired Reflexes (+2) already write into `system.movement.disengage` over Draw Steel's own initial value of 1. The **VOIDMARK index was last built at 0.3.104**, so rebuilding it picks up twenty chapters of RAW that shipped since — “What are the disengage rules?” now comes back with the combat chunk as the top hit. **The Field Surgery Kit and the Slap-Doc Kit were inert treasure rows**; both now run on the `consumableUse` seam the street chems already use, which needed two things it did not have: a **target other than yourself** (Stamina, Recoveries, conditions and buff effects land on the target; the once-per-combat ledger, the Taint and the spent unit stay on the user; nothing targeted resolves on you, two targeted refuses) and a **refusal before the card posts**, because stabilize means nothing on somebody upright. The Field Surgery Kit is a **reusable maneuver** that sets a dying target to **1 Stamina** per `04-combat` and is never spent; its *other* half — the edge on First-Aid / Restorative tests — is a plain `transfer: true` ActiveEffect writing `system.skills.modifiers.{medicine,medicineLore}.edges`, the same shape every chrome implant uses, which Foundry grants and revokes with the item for free. The Slap-Doc Kit hands back **two spent Recoveries** through the Trauma Patch's own ceiling rules and then offers a picker over the **physical** conditions actually on the target (Bleeding, Dazed, Prone, Restrained, Slowed, Weakened — not fear, taunts or surprise, and not grabbed, which ends when somebody lets go); the empty canister deletes itself, which the chems deliberately still do not. **Scout Conceal did nothing because of one character.** Its tier block's pseudo-document id was `gwConcealTiers0` — fifteen long, where Draw Steel's `DocumentIdField` takes exactly sixteen — so the whole `other` power-roll effect was invalid and the card rolled with no tiers attached. It was the only invalid `_id` in the module, and the wave smoke now asserts that across every row in `src/packs` so the next one is caught before the table finds it. The 2d10 + agility roll behind the Reflex label was already right. **Raven Beast form is 0.5 × 0.5**, like Rat and the sprites; Wolf stays absent on purpose. **Kade's gear drops to his own echelon** — he shipped with an E2 battledress and an E3 **two-handed** battle rifle, which a Warframe cannot hold alongside the shield its doctrine asks for, so he now carries a Security Rig, a Riot Shield, a Shock-Stick and his Hand-Cannon, all E1; the chrome and the 25 Body Integrity behind it are untouched. **Hold the Line stopped colliding** — the Operator heroic and the Warframe kit signature shared a `_dsid` and a name; the Operator keeps both and the kit signature is now **Frame Lock**. **Medic Reagents got their ceiling and their refill.** The no-drip half already worked (`patchPersistentReagents`), and so did spending — Draw Steel's own `AbilityModel#use` debits every heroic's printed 1/3/5/7/9/11 and every signature's 2+ enhance, and `enforceHeroicResourceCost` already refuses a use the bag cannot pay for, in *and* out of combat. What was missing was the other direction: **nothing capped a gain**, so a `[[/gain 2 hr]]` link, the Reputation feature or a Director's keystroke could bank 30 Reagents at Echelon 1, which `15-medic.md` forbids in as many words. The cap is now one `preUpdateActor` clamp at **10 / 14 / 20 / 38 by echelon, +2 for Advanced Chem-Prep** — every granting path goes through it, and it only ever clamps down. **Craft Reagents** is a new 1st-level class feature that puts a **downtime crafting Project** on the sheet: points in during respites, and on completion the kit refills to full capacity and the project resets for next time. The Director still quotes the ¥, and the restock discounts still apply to that quote. **Improvise!** now actually hands two Reagents back, gated on its printed trigger (3 or fewer) and once per encounter, trimmed to the ceiling. Diagnose's enhance joined First Aid and Administer Dose at **2+** rather than exactly 2. Packs rebuilt with **Foundry closed**. Smokes: `node tools/wave-03125-smoke.mjs` (the new one), `node tools/voidmark-smoke.mjs`, `node tools/pregen-regen-smoke.mjs`, `node tools/next-build-wave-03122-smoke.mjs`, `node tools/kiosk-smoke.mjs`. Foundry checklist `docs/directors/03125-smoke.md`.
- `0.3.124` — **Scout Conceal, Street Priest cleanup, and two more pregens.** **Every Scout gets Conceal, free** — a new 0-Advantage maneuver granted from the class's own L1 Features grant, so Hunter, Ghost and Face all walk in with it. Low does nothing; middle applies **Cover/Conceal** (F13's status, reused — no second cover effect was invented); high applies Cover/Conceal **and Invisible**, and the Invisible lasts **only until you move**. That last clause is the whole design: cover is about where you are standing, invisibility is about not having just given yourself away, so a square change — your own movement, a shift, a teleport, a forced move — strips the Invisible and leaves the Cover/Conceal exactly where it was. Draw Steel ships ten conditions and none of them is invisibility, and Foundry's vision pipeline only hides a token for the status id named by `CONFIG.specialStatusEffects.INVISIBLE` — so `scripts/conceal.mjs` registers `ghostwire-invisible` **and points that special at it**, which is what makes `basicSight` stop drawing the Scout and the `seeInvisibility` band every Ghostwire optic already grants (F20) start drawing them again. Mechanically invisible, not an icon. The card applies its own result off the tier Draw Steel reported, and a natural 20 reads as tier 3, so a crit vanishes you. **Hard Tag is never free** — the Hunter's mark was a free signature maneuver with an optional surge spend on top; it now costs **1 Advantage** every time, and the surge rider buys surges with Advantage spent *beyond* that first one. Marked, the first-strike bonus and the Marked effect are untouched. **Lightfall is out of the Street Priest pick pool** — Blessed Light has been the class's 20-foot lamp since 0.3.123 and the burst-teleport signature was competing with it; the ability JSON stays (the book and older worlds reference it) but nothing grants it and Vessa no longer carries it. **Sacrificial Offer is Dark Pact only**: the row now carries the same `flags.draw-steel-ghostwire.pact` that already gates Roster of the Saved, so `patchPactFilter()` hides it from a Light priest in chargen with **no new dialog code**, `planPactOptionals()` strips it from a live priest who swears Light — removal only, because an optional pick is the player's to make again — and the pregen generator honours the same flag, which is why Vessa regenerated onto **Warrior's Prayer and Word of Rebuke**. **Feral Cry deals 1 / 2 / 3** (was 2 / 5 / 7); the push riders are unchanged, and both Changer pregens' embedded copies agree. **Sprites are half a square** — all twelve templates place at **0.5 × 0.5**, and `compileSprite` writes the size itself so a world holding a pre-0.3.124 compendium copy still gets it. **Pregen names lost their street handles** — the Name field read `Barak Voss-Hallor — “the Foreman”`, which put a nickname in every actor list, token nameplate and chat speaker; all seven now carry the hero's name alone and the handle opens the Biography as `Street name: “the Foreman.”` The fiction index moved it to its own column and is now **The Nine**. **Two new Level 1 pregens**, built by the same generator as their siblings so a regen is still a byte-for-byte no-op: **Renn “Patchwire” Solace-Ward**, Medic (Street-Doc) · Pure Human, on the class's own Quick Build kit with the bag, no chrome and no licence; and **Kade “Hardframe” Orrin-Vex**, Operator (Corp-Milspec) · **Cyborg**, who spends every one of a Cyborg's **25** Body Integrity — Wired Reflexes 6, Cyberlimb Arm 5, Muscle & Bone Lacing 5, Dermal Plating 4, Cyber-Eyes 2, Cyber-Ears 2, Datajack 1, all Standard grade, **0 remaining**. Kade is the first Cyborg pregen, so the generator no longer hard-codes Body Integrity at 20; `INTEGRITY_BY_ANCESTRY` mirrors `CYBORG_INTEGRITY_START` in `scripts/module.mjs`. Michael's own portrait and round-token crops for both landed with the wave, so the R2 pipeline carries them (512² → 1024² yuva420p, re-masked) rather than either of them shipping art-less. Packs rebuilt with **Foundry closed** (`classes`, `origins`, `summons`, `pregens`, `pregen-fiction`, `rulebook`). Smokes: `node tools/scout-sp-wave-03124-smoke.mjs` (the new one), `node tools/pregen-regen-smoke.mjs`, `node tools/r2-pregen-round-tokens-smoke.mjs`, `node tools/sp-el-foundry-wave-03123-smoke.mjs`. Foundry checklist `docs/directors/03124-smoke.md`.
- `0.3.123` — **Street Priest rework, Elementalist Cantrip, and the Foundry fix wave.** **One free strike per priest, and the pact picks it** — the class shipped *two* free ranged strikes (*Smite / Rebuke* as an always-granted class signature and *Rebuke* again in the optional pool) and Drain was a pick anybody could take. `smite-rebuke.json` is **deleted**; the surviving card is **Rebuke**, deliberately low power at **2 / 4 / 6 + Persona** holy with its pull rider, carrying the Conviction spend options Smite / Rebuke used to own. A new **Pact Strike** advancement (choose 1 of 2) sits after the free class signatures, and because both rows carry the existing `pact` flag the chargen filter that already gates Roster of the Saved / Ledger of the Damned does the work — **no new dialog was written**. **Light gets Rebuke, Dark gets Drain, nobody ever has both**, and live heroes are swapped by `scripts/pact-strike.mjs` when they swear a pact or when a pre-0.3.123 world loads. **Blessed Light is a lamp, not a weapon** — it was a 3/5/8 holy ranged strike, which left the class with two free ranged strikes and no light source; it is now a **free maneuver, no roll, no damage**, raising a **20-foot bright light** on your token until you use it again to put it out, and it moved into **Class Signatures** so every priest has it at 1st level. The radius is converted at the scene: `scripts/token-light.mjs` reads `grid.units` / `grid.distance` and turns 20 feet into **20 on a foot-scaled map or 4 squares on a Draw Steel one**, with `bright` and `dim` set to the *same* number because the card prints one edge — and the token's previous light is snapshotted first, so a Director's own torch survives the round trip. **Minor Rebuke is Holy Smite** — same 1-Conviction ranged main action, but **3 / 5 / 8 + Persona** instead of none / 2 / 4, because the old numbers made spending a point do *less* than the free strike it sat under. **Lay On Hands and Sense the Veil are untouched.** **Elementalist Cantrip** — a **fourth** signature granted with the other three at 1st level, **0 Essence, never boostable, no damage ever**. Using it opens one dialog: **Light** (the same shared 20-foot glow, toggling) or **Other utility** (a description box — scour a coat, flick a switch across the room — posted under the card with nothing mechanical applied; an empty box is not a cast). **Armor fills the pool** — donning a suit now sets current Stamina to the new max; removing one **never heals** and only clamps if the max actually dropped. **Trauma Patch is usable** — flavour-only treasure until now, it spawns a maneuver and **restores 1 spent Recovery** (not Stamina — you still choose when to cash it in), **once per combat**, tracked by the Combat's own id so a new fight is a new allowance and **outside combat there is no gate**; the refusal lands *before* the card posts, and the patch is consumed either way. **Your own hero pins to the top of Wired Console Connections**, tagged *(you)*, resolved from the Actor assigned to your User (one owned hero as the fallback; genuine ambiguity pins nothing). **Compile Sprite actually compiles** — the archetype picker and the summon have existed since B52 but only on the sheet button, so *using the card* just rolled; it now runs the real thing through the same `createChatMessage` seam the Veil summons use, with a world setting to go back to roll-only. **Recompile rebuilds** — it had no power effects and no behaviour; per RAW it now **reshapes** a standing sprite into another archetype at full power in the same square, **or rebuilds one that was just destroyed**, where it fell, **at half Stamina** (rounded up, never below 1), with the destroyed sprite remembered on the caster until the fight ends. Packs rebuilt with **Foundry closed** (`classes`, `gear`, `pregens`); Vessa and Kaïs regenerated through `tools/pregens-to-actors.mjs` so their embedded copies agree and the regen stays a no-op. Smoke: `node tools/sp-el-foundry-wave-03123-smoke.mjs` (153 checks), plus `node tools/pregen-regen-smoke.mjs`. Director note `docs/directors/sp-el-foundry-wave-03123.md`; Foundry checklist `docs/directors/sp-el-foundry-wave-smoke-03123.md`.
- `0.3.122` — **Twelve locks in one ship — the next-build wave.** **Changer sheet** — the three form buttons stay on Stats, where a mid-fight maneuver belongs; R2's six art thumbnails (a portrait picker *and* a round-token picker for each form) moved to the **Biography** tab, because they had pushed Stamina and the characteristics below the fold. Same flags, same `syncChangerFormArt`, one dim pointer line left behind so nobody hunts for them. **Director: +1 Heroic Resource** — a new macro that adds **1** to a hero's class-named primary (Influence, Adrenaline, whatever the class calls it) on targeted, else selected, tokens. Not Surges, not Victories. It writes through Draw Steel's own `system.updateResource(+1)` rather than the field, so the class minimum and the `modifyTokenAttribute` hook both still fire, and it reuses the same targeted-beats-selected helper `Director: Taint +1` already shipped. **Beast-Hide re-pick** — the card says you may change your damage immunity when you finish a respite and the advancement has said `repick.respite: "finish"` all along, but Take Respite does not open the chooser in practice. Right-click the trait → **Re-pick this choice**, which opens Draw Steel's **own** `effectGrant` dialog over the **stock six-immunity pool** and lets `reconfigure()` do the delete-and-recreate. No immunity math was invented and no pool is hard-coded, so **Layered Hide** came along free — the rule is "any owned Item with a re-pickable effectGrant". The entry sits inside the sheet's existing row menu rather than a second menu fighting the first for the same click. **Wire State gained a Disconnect rung** — the S9 picker was three rungs and argued hard that Disconnected must never be a fourth; Michael retired that doctrine. It is now **Disconnected / Linked / Overlay / Jumped In**, and what survived is the rule the old doctrine was protecting: going *on*-net still runs the **same `actorHasConnectInterface` check the Connect verb runs**, coming *off*-net needs nothing, leaving the seat still calls `jumpOut()` first, and Jump-In is still gated on one capable machine. **Cycle Wire State** — a macro that steps every selected token one rung along Disconnected → Linked → Overlay → Jumped In → Disconnected, independently, so a refusal toasts without aborting its siblings; it performs nothing of its own and hands the rung to `applyWireState`. **Stamina tooltip** — hovering the Stamina pool on a locked Hero sheet now lists every contributor to max: class (`starting + (level−1) × per-level`), the **best** kit × echelon the way Draw Steel computes it, **worn armor**, traits and other effects, with `upgrade`-mode changes counted once rather than summed — and anything it cannot account for shown as its own honest **Other effects** row, so the printed total is always the sheet's real `system.stamina.max`. **Changer Darksight 30** — a People trait the Changer ancestry grants automatically, wired through the **F20** `sightGrant` path: stock Foundry `basicSight` at range 30 plus the `darkvision` vision mode, the same `nightOptics` mapping Low-Light Goggles use. No custom DetectionMode, and it deliberately does **not** flip Has Vision — `token-vision.mjs` still owns that. Audited into the F20 smoke's sight table so it can never drift. **Trade Cant is free** — every hero gets `caelian` on the create-Actor path (additively, so duplicates keep their lists), existing worlds get a one-time flagged grant, and the Chargen Wizard lists it as **(free)**, never counts it against the language budget, refuses to let it be given up, and restores it on Start Over. **Machine conditions** — a deployed frame's Integrity now drives three bands: **On Fire / Leaking** at ≤50%, **Crippled** at ≤25%, **Systems Down** at 0%. One band at a time, worst wins; they are real statuses so the token shows them, and the Machine sheet's first page prints `Condition: … (38% Integrity)`. A frame with **no** max Integrity reads **Unrated**, not Systems Down — 0/0 is not a wreck. **Stalled and Dead-stick are out of scope and are not modelled.** **Rat Beast tokens are 0.5** — the canvas half of a fact `rat-lineage-trait.json` already states in the rules (size 1S). The pre-Beast footprint is snapshotted once, so Human and Hybrid restore a Director's own 2×2 frame rather than normalising it to 1×1; Wolf and Raven print no size override and are never touched; lineage is read from the existing `changerLineage` flag. **Advanced Tactics costs 1 Influence** — `resource: null` → `resource: 1` on the Commander Street Fixer ability, with **Barak** regenerated through `tools/pregens-to-actors.mjs` so his embedded copy agrees and the regen stays a byte-for-byte no-op. **Worn armor finally adds Stamina** — every armor SKU has always shipped four `No Kit — Echelon N` effects on `system.stamina.bonuses.treasure` and **every one of them shipped disabled with nothing that ever enabled one**. Armor was pure flavour. Now `flags.draw-steel-ghostwire.worn` says what is on your body — new armor on a hero wearing none is worn automatically, and a right-click entry wears or removes one — and exactly **one** echelon band is ever enabled: **one armor, newest wins**, shields included (they carry the identical bands and live in the same folder; one constant flips that if Michael wants them to stack). **Kit Stamina is untouched and no kit effect is ever disabled** — the bands are only *named* "No Kit" because that is what the packs shipped, and the picker already prefers a `With Kit` sibling if one ever ships. Echelon clamps rather than fails and re-picks on level-up; catalog copies in the pack stay disabled; pre-0.3.122 worlds get a one-time retroactive pass. Packs rebuilt with **Foundry closed** (`origins`, `classes`, `macros`, `pregens`). Smokes: `node tools/next-build-wave-03122-smoke.mjs` (the new one), `node tools/wire-state-toggle-smoke.mjs` (rewritten for the four-rung ladder and the cycle), `node tools/f20-foundry-sights-smoke.mjs`, `node tools/pregen-regen-smoke.mjs`. Director note `docs/directors/next-build-wave-03122.md`; Foundry checklist `docs/directors/next-build-wave-smoke-03122.md`.
- `0.3.121` — **R2: the pregens stop walking onto the map as rectangular photos.** Every pregen canvas token is now a **round 1024² WebP with transparency** under `assets/tokens/pregens/`, cut to match the bestiary / ARG / mule-bot plates the rest of the module already uses. **Sheet portraits are untouched** — the Hero sheet still shows the square dossier plate under `assets/pregens/`, and those files were neither rounded, overwritten nor replaced. The split is the whole change: `img` and `prototypeToken.texture.src` used to be the *same path* on all seven heroes, and `syncChangerFormArt` forced them to stay the same on every Changer form swap. They are two fields with two sources now, and the runtime never writes one onto the other. **Eleven plates**: Barak, Kaïs, Kessic, Sabbat and Vessa from their existing print circles under `docs/manuscript/print-art/pregens/` (upscaled 512→1024, alpha re-cut); Vira **human / hybrid / beast** and Wren **default / hybrid / beast** so both Changers swap the canvas as well as the sheet — Wren had no `*_circle` at all, so hers are cut from the 1024×1536 print portraits. Changer flags carry both halves: `humanArt` / `hybridArt` / `beastArt` stay the square portraits, and new `humanToken` / `hybridToken` / `beastToken` carry the circles; a form with no `*Token` falls back to its `*Art`, so hand-built Changers keep working. The Hero sheet's Changer Forms box gained a **round-token picker beside each portrait picker**, and clearing one no longer strands the other. Generation is reproducible and **ffmpeg-only** — `tools/pregen-round-tokens.mjs` crops each source square (`focusX`/`focusY` pan the crop; tall plates anchor to the top, the way the print circles were cut), scales to 1024² lanczos, and cuts the alpha disc with `geq` and a 1.5px feather so the rim does not stair-step on a 100px grid. **Windows `convert.exe` is not ImageMagick and is never called.** Pregens pack rebuilt with Foundry closed. Smokes: `node tools/r2-pregen-round-tokens-smoke.mjs` (the new one — it reads the alpha plane back through `alphaextract` and fails a square plate on coverage), `node tools/pregen-regen-smoke.mjs` (its old `token === portrait` assertion is now the *split* assertion, and the regen is still a byte-for-byte no-op). Director note `docs/directors/r2-round-pregen-tokens.md`; Foundry checklist `docs/directors/r2-round-pregen-tokens-smoke-03121.md`.
- `0.3.120` — **R0: the Hero sheet stops being a stock Draw Steel sheet.** Michael locked the design at `docs/directors/ghostwire-r0-hero-sheet-mock.png` and this is that mock in CSS: **deep ink** ground, a **muted cold-steel** accent (`#6a8a9a` — hue 200°, saturation 19%, deliberately *not* the module's neon `#6ee7ff`), a large soft **GHOSTWIRE watermark** set in a display serif behind the header, a **hairline wire frame** with a node dot at each corner and a short trace run off two of them, one small **corner sigil** bottom-right, and **muted ember** (`#8a5a3a`) reserved for **Body Integrity** and the Taint bands and nothing else. Scope is one class: `scripts/module.mjs` stamps `ghostwire-hero-sheet` on the Hero sheet root in a `renderDrawSteelHeroSheet` hook — one line, and the only JavaScript R0 adds — and every selector in the new block sits under it. **The applets are not repainted:** `--ghostwire-accent` is still the neon cyan, the Wired Console / Chargen Wizard / Run Generator / Ritual Working / kiosk / locker / machine sheet all still read it, and the smoke asserts the R0 block never reads it at all. The lever that does most of the work is re-pointing Draw Steel's own `--draw-steel-c-*` variables on the Hero sheet root, which carries tags, rules, table headers, item stripes, placeholders and button hovers into deep ink in one move without touching a line of system layout HTML — and means the sheet looks the same on the light Foundry theme as on dark. The corner sigil is a **975-byte** SVG (`assets/brands/ghostwire-sigil.svg`) used as a **`mask-image`**, so the glyph takes the steel token rather than baking a colour in; **no pack was rebuilt.** Watermark, frame, trace and sigil are all `pointer-events: none` — atmosphere never eats a click. **G3's two deferrals are closed:** the letter-spacing scale is now the ten-rung `--ghostwire-track-*` ladder, the per-applet panel darks are one `--ghostwire-panel-0…10` ramp sorted by *relative luminance* rather than by hex (step 0, `#05080c`, is new — the sheet's ink ground), and the four wire-state colours are named. **106** declarations moved onto them. The two greens stay two greens on purpose: the console's Linked rung is read against its own cyan and the sheet's against steel, so merging them would be an applet repaint R0 has no remit for. Also swept up, because R0's acceptance needs the G3 smoke green and it was **red on main**: two `var(--ghostwire-accent, #hex)` fallbacks the 0.3.106 machine sheet had re-introduced, and five re-typed radius-ladder sizes. Smokes: `node tools/r0-hero-sheet-skin-smoke.mjs` (64 checks), `node tools/g3-lang-style-tokens-smoke.mjs`. Director note `docs/directors/r0-hero-sheet-skin.md`; Foundry checklist `docs/directors/r0-hero-sheet-skin-smoke-03120.md`.
- `0.3.119` — **Sight SKUs change the canvas, and Veil/Incursion attacks the Cortical Firewall can actually stop.** **F20** — equipping Cyber-Eyes or Low-Light Goggles now rewrites the token's `detectionModes`, not just `sight.enabled`. A sight SKU declares `flags.draw-steel-ghostwire.sightGrant` and `scripts/sights.mjs` recomputes `prototypeToken.detectionModes` plus every placed token on Item create / update / delete and on the Active Effect hooks. **Stock Foundry modes only** — no custom DetectionMode, no thermal shader — and exactly three claims: night optics → `basicSight` + the `darkvision` vision mode, thermal → `seeAll`, Veil glimpse → `seeInvisibility`, each at a **printed range in squares**. Foundry ships no thermal imager, so Ghostwire *defines* thermal as `seeAll` at a short range (creatures through smoke, darkness and invisibility; walls still block) and prints that sentence on every card that claims heat. Cyber-Eyes and Cyber-Eyes (Soft) 10 / 5, Low-Light Goggles 10, Thermal Scanner 10, Sensor Sweep Drone-Eye 10 / 10, Full Sensorium 20 / 10 / 10, Deep Optics 5 / 5, Sensor Pod 10 / 5, Taint Sight 10, Predictive Sensors 20. Two optics take the **better** band, never the sum. **A grant is only live while its source is**: F12 suppressed or destroyed chrome sees nothing, and a mod grants nothing until it is installed and switched on. Everything written is recorded in `flags.draw-steel-ghostwire.sightApplied`, so removing the implant removes exactly those modes and leaves a Director's own configuration underneath. Cheap Shades and Thermoptic Skin grant nothing and **say so**; Penetration Optics, Detect the Supernatural and Cold Read have no stock mode and their cards now print **Director call** with the reason. Sensor Pod is the one SKU that also switches Has Vision on — a drone with no vision would read nothing through its own pod — and it never overrides `token-vision`'s scope. **F21** — Cyborg **Cortical Firewall** (psychic immunity = level) finally has something to absorb. Ghostwire registers a `psychic` ability keyword beside its `tech` / `chrome` / `optics` / `wired` / `command` (Draw Steel's `psionic` is the Talent class keyword, and the firewall keys off psychic **damage**). The doctrine: psychic marks an attack whose only medium is the mind, and where the medium *is* the mind the damage type is psychic too. Ghost **Heat Death** (7 / 10 / 13) and **Awful Wail** (3 / 5 / 8) are retyped from cold and sonic to **psychic** and tagged; **Shriek stays sonic** on purpose. The ghoul gains **Hunger Bleed** (Malice 3, burst 2, 2 / 3 / 4 psychic, weakened / frightened), banded off Zombie Dust; the Veil Cultist gains **Show Them the Debt** (Malice 2, ranged 10, 4 / 6 / 9 psychic, frightened), banded off its own Blight Grenade. The Hunter Spirit's **Binding Chain** deals psychic under either pact — the chain is a name, not metal — while the Warrior's Pact Blade still tints holy or corruption. The Signal Talker (Invader)'s Psionic Boom and Memory Thief pick up the marker keyword on damage they already rolled. **Skeleton and Zombie stay entirely physical**: `soulless` is the line — an undead with nothing behind its eyes has no mind to push with — and their cards say so. Razor Claws, the Hexed Revolver and every tentacle stay as they were. Scripts / lang / RAW notes plus one regenerated pregen (Kessic's cyber-eyes) — **chrome, gear, mods, origins, classes, bestiary, summons and pregens packs rebuilt**. Smokes: `node tools/f20-foundry-sights-smoke.mjs`, `node tools/f21-psychic-incursion-undead-smoke.mjs`. Checklists: `docs/directors/f20-foundry-sights.md`, `docs/directors/f21-psychic-incursion-undead.md`.
- `0.3.118` — **Smoke reds cleared and three RollTables get icons that exist.** **Token vision** — `SKIP_TOKEN_VISION_KINDS` gains `baseAsset` and `locker`. F18 Base Assets (Door Lock, Safehouse Beacon, Sentry Turret, the workshop benches) and F10 Lockers are placeable stubs, not sighted creatures, so they keep `sight.enabled: false` alongside kiosks, nodes, vehicles and drones instead of having Has Vision forced on at create or by the ready migration. **S8 machines smoke** — base assets are no longer held to the buildable-chassis rules. They deliberately ship with no Fabricate Project card and no machine-mod-slot paragraph, so the Fabricate-paragraph and 24-mod-link checks now run over `buildableChassis` only; price and stock Project-field checks still cover every base asset, because the benches carry real Project goals. The carve-out reads `vehicle.baseAsset` as truthy rather than `=== true`, since the Safehouse Beacon stores its own dsid there and `scripts/machines.mjs` reads that string. **RollTable icons** — City, Flats and Cyborg System Crisis pointed at `icons/environment/city/skyline.webp`, `icons/environment/city/city-night.webp` and `icons/commodities/tech/blue-cog-bolt.webp`, none of which ship with Foundry 14, so each table opened with a missing-image alert. They now use `icons/environment/settlement/city-night-spire.webp`, `icons/environment/settlement/city-night.webp` and `icons/commodities/tech/cog-brass.webp`; `tools/encounters-to-tables.mjs` carries the two zone paths so a regen keeps them, and the same generator now preserves the lang keys of hand-authored KEEP tables instead of dropping F15’s Cyborg System Crisis name on every rebuild. **Encounters pack rebuilt.** Smokes: `node tools/token-vision-smoke.mjs`, `node tools/s8-machines-smoke.mjs`, `node tools/f15-cyborg-crisis-smoke.mjs`.
- `0.3.117` — **Criticals become Draw Steel criticals, Papermill soft cut, Flanking, Cyborg System Crisis, and workshop benches.** **Crit** — natural doubles are retired from RAW; a critical is a natural **19 or 20**, it is always read at **tier 3** whatever the total came to, and a critical on a **main-action** ability hands back a main action on the same turn. The chat card gains a **Critical** badge, the rule card gains a one-press affordance for the extra main action, and `criticalTierProblems()` is a tripwire that would announce a future system change rather than quietly pay tier-1 damage on a natural 20. **F23b** — the 0.3.116 ladder priced illegal paper like crime and played too steep, so the Restricted and Military bands take a soft cut: Burn holds the floor at ¥500, Lanyard ¥1,800, Paper Ghost ¥2,200, Clinic ¥5,000, Carry Permit ¥7,000, Broker ¥9,000, Wire Ticket ¥18,000, Deep Cover ¥30,000, Cradle-Seeded ¥75,000. Every kiosk gains a **Card** button that opens an item's sheet so a player can read the rules without buying, and because that sheet can be edited, `flags.draw-steel-ghostwire.<family>.price` is now **Director-only** — a `preUpdateItem` guard strips a non-GM price write and lets the rest of the update through. **F14** — two enemies on opposite adjacent sides of a target give **melee strikes** one **edge**, auto-detected from token positions, never a player toggle. Draw Steel already ships flanking, so Ghostwire does not re-implement it and guards against ever adding a second edge on top of the system's; what it adds is a generic `cannotBeFlanked` Actor flag, the rule that a **meat-inert** Rigger body is not holding a side, the flank the system drops when the attacker's token is not selected, and a computed **Flanked** marker. **F15** — a full Cyborg who would enter the dying track enters **System Crisis** instead: no dying saves, no dying strikes, magic healing does nothing, and the Director rolls **2d6** on the new **Cyborg System Crisis** RollTable in the encounters compendium for Brick / Cascade / Limp-Home / Soft Reboot / Failover. Crisis is explicitly **not** F12 chrome damage applied to the whole Cyborg, not a Body Integrity debit, and never deletes an Item. **F18** — six expensive, Restricted, placeable **workshop benches** (Armorer's ¥8,000, Weaponeer's ¥10,000, Matrix / Deck Lab ¥15,000, Ritual Sanctum Tools ¥16,000, Chrome Bay ¥18,000, Vehicle Depot ¥22,000) as dual Item + Actor Base Assets. Each gives **+1 Lifestyle project slot — capped at +1 in total however many are placed** — plus **one edge** on a matching-craft-family Project. The edge is opt-in: tag a Project's craft family from its context menu and a matching bench pre-seeds the edge into the roll dialog; untagged, the Director adds it in one click, because guessing a Project's family from its name would hand out edges on a string match. RAW / scripts / lang / CSS / kiosk template — **gear, encounters, rulebook, summons and vehicles packs rebuilt**. Smokes: `node tools/crit-feedback-smoke.mjs`, `node tools/papermill-pricing-smoke.mjs`, `node tools/f14-flanking-smoke.mjs`, `node tools/f15-cyborg-crisis-smoke.mjs`, `node tools/f18-workshop-benches-smoke.mjs`. Checklists: `docs/directors/papermill-pricing-03117.md`, `docs/directors/f14-flanking.md`, `docs/directors/f15-cyborg-system-crisis.md`, `docs/directors/f18-workshop-benches.md`.
- `0.3.116` — **Wire-state toggle, Papermill pricing, Critical Roll feedback, and Cover/Conceal.** Four table-facing gaps in one build. **S9** — a Rigger picks between **Jumped In**, **Linked** and **Overlay** from a broadcast-tower button on the token HUD or a chip beside the hero’s name, and it writes **the same flags Jump-In, Deploy and the Wired Console already write**: Linked / Overlay go through `setWiredState()`, Jumped In goes through `jumpIn()` / `jumpOut()`. **Jumped In is not Jacked In** — both carry `ghostwire-jacked-in` and both stay full Connected for the F12 Pulse path, but only Jumped In carries meat-inert, and leaving the seat by the toggle runs the real Jump-Out cleanup before landing in the rung you picked, so `jumpedInto` is never stranded. Disconnected is refused — Connect is still the only on-ramp and Jack Out the only off-ramp — and `tools/wire-state-toggle-smoke.mjs` asserts the *absence* of a parallel flag namespace as hard as it asserts the door. **F23** — the Papermill ladder is re-cut so illegal paper feels illegal: Burn stays the floor of existence at ¥500, street forgeries roughly double (Lanyard ¥2,500, Paper Ghost ¥3,500), the Restricted middle takes the pass hardest (Clinic ¥8,000, Carry Permit ¥12,000, Broker ¥15,000), and the premium end bumps (Wire Ticket ¥28,000, Deep Cover ¥45,000, Cradle-Seeded ¥110,000). One vendor, unchanged. **F11** — a natural 19 or 20 on a power roll now plays a sting, flashes the canvas and posts a short Ghostwire-worded rule card saying what that critical gives; detection walks the message’s rolls and its Draw Steel chat parts and reads `dice[0]`, never a total, so three edges and a kit bonus can never fake one and a 19-damage roll is not a critical. B40 ability SFX is untouched and the crit sting reuses its volume setting; the flash is client-scoped. **F13** — one token status, **Cover/Conceal**, that puts a **bane on ranged attack power rolls targeting that token** and leaves melee alone. It rides Draw Steel’s own `getTargetModifiers` seam, so in a multi-target attack only the covered targets take it and the player sees it in the roll dialog before the dice move. No Flanking, no cover tiers. Scripts / lang / CSS plus nine gear prices — **gear pack rebuilt**. Smokes: `node tools/wire-state-toggle-smoke.mjs`, `node tools/papermill-pricing-smoke.mjs`, `node tools/crit-feedback-smoke.mjs`, `node tools/cover-conceal-smoke.mjs`. Checklists: `docs/directors/wire-state-toggle-03116.md`, `docs/directors/papermill-pricing-03116.md`, `docs/directors/crit-feedback-03116.md`, `docs/directors/cover-conceal-03116.md`.

- `0.3.115` — **Wrenches pick one Echelon 1 drone for free at chargen.** The rule was in the text and had never existed in Foundry. The Chargen Wizard gains a **Free drone** step immediately after **Kit**, and *only a Wrench sees it* — for every other class the step is absent from the rail, absent from next/back, and absent from the Done checklist, so a non-Wrench reads `… / 13` exactly as before while a Wrench reads `… / 14`. The picker lists **every Echelon 1 drone chassis** in the Vehicles compendium and nothing else — fifteen today (Buzz, Crawler, Fly, Junkbug, Mule-Bot, Needle, Rattlebox, Rotor, Rustbucket, Sink-Floater, Skitter, Spotter, Sputter-Sled, Tape-Eye, Taser-Bee); no Echelon 2+, and no vehicle that is not a drone. **It is free, and free is structural.** `grantFreeChargenDrone()` uses the same Item path a purchase uses (`game.items.fromCompendium` then `Item.create` on the hero) and deliberately never names `WEALTH_PATH`, `planChargenSpend`, `getWealth`, `catalogPrice` or the chargen `spent` counter — the smoke asserts the absence of all five in the grant body *and* their continued presence in `buyChargenItem`, so the two paths cannot quietly converge. One pick, held by two locks the way Kit grants are: `flags.draw-steel-ghostwire.chargenFreeDrone` on the Actor as the ledger and the same flag stamped on the granted Item. A Wrench who wants no chassis waves it off with the same ack the Kit step uses; **Start over** on the step hands it back with nothing to refund. Separate from Kit street-band grants and from the ¥5,000 early spends — vehicles stay buyable there, so a second drone on day one is still the runner's own money. Scripts / template / lang / CSS — **no pack rebuild**. Smoke: `node tools/wrench-free-drone-smoke.mjs`. Checklist: `docs/directors/wrench-free-drone-chargen-03115.md`.

- `0.3.111` — **Install chat card, Jump-In denial, seat abilities while Jacked In, and an empty fleet returns to Linked.** Draw Steel hid the 0.3.110 install message because a `standard` chat with no `content` part never reaches the log; Install onto… now posts that part. **Jump-In (Signature Platform)** rolled without calling `jumpIn`; a non-capable Bulldog now gets `JumpInNotCapable` and no roll. While Jacked In, the meat-roll gate was refusing every non-Wired power roll, including **Deploy & Command**. Seat abilities (Deploy & Command, Rigged Fire, Field Repair, Focus Fire, fleet and platform actions) stay usable. Personal weapons and other classes stay blocked. Meat inert stays. Recalling or deleting the last fielded drone, vehicle, or base asset clears Jump-In and leaves the pilot **Linked** (not Jacked In, and not Disconnected). Overlay, a Matrix **Jacked In** with no machine seat, and a hero who was never on the wire all stay as they were. Smoke: `node tools/rigger-vertical-smoke.mjs`. Checklist: `docs/directors/mod-install-chat-03111.md`.

- `0.3.110` — **Heavy Hardpoint on the machine sheet, an install chat card, and drones always Jump-In capable.** Installing a mod still records it on the hero's chassis Item. Weaponry kits (Heavy Hardpoint, Gun Rack, Twin Mount, Turret Ring) only stamped `installedKits` and an Active Effect the Machine sheet does not list, and Deploy then wrote an empty Installed mods line over the Build tab. Sync now mirrors every installed chassis mod onto the deployed Actor (Inventory lists the real Item) and fills Build → Hardpoints / Installed mods from that set, including when Deploy's later stamp runs. A successful Install onto… posts a chat card (who, mod, host, slots, and the fielded machine when one exists). Payload magazine loads keep their own card. **Every drone** (kind `drone`: band prototypes, Mule-Bot, and Deploy) is Jump-In capable without a chassis flag or Rigger Cocoon. Vehicles and base assets keep their own flag. Smoke: `node tools/rigger-vertical-smoke.mjs`. Checklist: `docs/directors/machine-hardpoint-install-03110.md`. Pack rebuilt: summons.

- `0.3.109` — **Machine Scorch Marks.** Deployed drones, vehicles, and base assets stamp Monk's Bloodsplats `flags.monks-bloodsplats.bloodsplat-type` = `scorch` on the Actor prototype and the placed token (other bloodsplat flags are kept). The same flag is on machine Actor prototypes in Summons and on Nox's trash freighter. Living heroes and NPCs are unchanged. Chassis Items have no prototype token; Deploy forces the flag. Smoke: `node tools/rigger-vertical-smoke.mjs`. Checklist: `docs/directors/machine-scorch-marks-03109.md`. Packs rebuilt: summons, deadhead (Foundry closed).

- `0.3.106` — **Machine sheet hot-fix.** Four defects from the 0.3.105 morning smoke, all traced to Foundry v14 core rather than guessed at. **Tabs** were dead because `ApplicationV2` intercepts `data-action="tab"` in its own click dispatcher *before* it consults `options.actions`, so a custom `tab` handler can never run — the sheet now rides the core tab group (`static TABS` + `tabGroups`), which also means every panel stays in the DOM and a Save from any tab writes the whole sheet instead of only the visible one. **Save Changes** threw *cannot read properties of undefined (reading 'flags')* because `_processSubmitData` is handed an already-expanded object, not a `FormDataExtended`; the tag / kind normalisation moved to `_processFormData` and core keeps its own submit path. **The stock NPC sheet kept winning** because `DrawSteelNPCSheet` extends `DocumentSheetV2` directly and never passes through `ActorSheetV2`, so the old `renderActorSheetV2` redirect was dead code — Deploy now stamps `flags.core.sheetClass` into the new Actor, a GM-side sweep back-fills machines deployed earlier, and a `renderDocumentSheetV2` guard catches the rest. **Fleet Size** counts by the `deployed` flag rather than by a successful `fromUuidSync`, so an Actor this client cannot resolve can no longer zero the count and wave a refused Deploy through; the owner is resolved from `item.parent`, an explicit argument, **or** `flags.ownerUuid`, so the refuse runs however Deploy was triggered; and the invented **Wide Band, Redoubled +4** is gone — per the Wrench master, Redoubled only drops the distance requirement on the whole-swarm Command. The 4th Deploy Michael saw was in fact correct: Drone Jockey grants **Wide Band at 1st level**, so that hero's cap is **5**, not 3 — the Deploy notice now prints `Fleet n / cap` so the rule is visible at the table. Also: Director notes on the Story tab are GM-only in the form (they are a `gmOnly` field, and a player's client would have submitted them back empty), and the header art reads as two captioned slots — Portrait and Token — instead of the same picture twice. Scripts / template / CSS / lang — **no pack rebuild**. Smoke: `node tools/rigger-vertical-smoke.mjs`, which now executes `fleetSizeCap` / `fieldedMachineCount` / `machineOwner` for real under stubs. Checklist: `docs/directors/rigger-vertical-smoke-03106.md`.

- `0.3.105` — **Rigger vertical slice.** Ghostwire Machine sheet (drone / vehicle / baseAsset) with required Description, Notes, portrait + token art; Deploy & Command inventory picker; Fleet Size refuse (3/4/5/6 + Drone Jockey Wide Band); Jump-In sets Jacked In + meat-inert AE and drains Uptime / biofeedback on machine hits; E1 Door Lock, Safehouse Beacon, Camera/Sensor Mast dual Item+Actor plus thin Sentry Turret / Mesh-Web / Barricade / Workshop Bench stub; chassis Integrity/Speed/Jump-In/Handling stamp from Item flags (bands remain fallback); Facility Rigger Home Ground edge AE when jacked into a Beacon. Smoke: `node tools/rigger-vertical-smoke.mjs`. Checklist: `docs/directors/rigger-vertical-smoke-03105.md`. Packs rebuilt: vehicles, summons (Foundry closed). Deferred: full drone AI, parallel Uptime UI, Allfather live pack pull if Foundry was open overnight.

- `0.3.104` — **Cyborg Body Integrity 25 + living Chrome, and chargen spend preview-before-buy.** Cyborg heroes use Body Integrity **max/start 25** (living Peoples stay **20**), may buy living Chrome from the chrome pack (install debits Integrity; removal refunds 75%), and no longer see Stats / chargen **N/A** or a chrome install block. Arcane Severance + Cortical Firewall / psychic immunity stay unchanged. Frame Modules remain a **deferred retag** stub of chrome SKUs — no new pack. Existing Cyborg actors migrate once to max 25 when they still have missing Integrity or max 20. Chargen Early Spends: each catalog row has a **View** (eye / name click) that opens the Item card without buying; Buy stays separate. Scripts / lang / templates / RAW — **no pack rebuild**. Smoke: `node tools/chargen-wizard-smoke.mjs`. Checklist: `docs/directors/cyborg-bi25-chargen-preview-03104.md`.

- `0.3.101` — **Chargen Wizard (B95 / I2): Appendix B, clickable, on the Hero sheet.** A **Chargen** button in the Hero sheet header — top of the sheet, clear of the tab strip — opens a **13-step** wizard that walks the Appendix B punch-list for you. Players and Directors both use it; the gate is Actor ownership, or GM. The order is Michael's FULL order: **Concept → Name → People → Background + Profession → Class → Kit → Skills → Characteristics → Languages → Resource + numbers → Body Integrity → Early spends → Done**. Step 1 is a free-text concept with **Ask VOIDMARK** as the primary action: the chat opens with a chargen question already typed from whatever you wrote and picked — read it, edit it, send it — and the wizard **never touches the thread mode**, so the B122 / S6 audience lock stands and a player asks as a runner. **It is a driver, not a second chargen engine.** Every spine pick makes the exact call the hero sheet makes on a drop — `system.advance()` for a class, `system.applyAdvancements()` for People / Background / Profession / Kit — so the stock advancement dialog runs, subclass / doctrine / pact prompts are the system's own, and no grant math is re-implemented. **G1 Kit grants fire by themselves**: take a Kit and the street-band package lands from `kit-grants.mjs`'s own hook; the wizard only reads G1's table to say whether a Kit wants gear. Where a prompt writes late, **Re-check** re-reads the sheet without closing the wizard. **The ¥ firewall is structural, not a reminder.** Characteristics are the printed **2, 2, 1, 1, 0** enforced as a multiset — the pool shrinks as you place numbers, so a third 2 is never in a dropdown — and **Lean on the class cores** drops the two 2s on the class's cores, deterministically. ¥ buys objects and nothing else: the spend catalog reads **four packs only** (gear, matrix, foci, vehicles), the **chrome and mods packs are named forbidden**, and `isChargenSpendable()` refuses a chrome flag on top of that — two locks, because `module.mjs` debits **Body Integrity** the instant a chrome Item lands on a hero, so “no chrome at chargen” has to hold at the *catalog*, not at the click. Unpriced and ¥0 rows are refused; Lifestyle is never pre-paid; buying a Personal/Light air scout still grants **Street Eye** through B94's own hook. Spends debit `system.hero.wealth` through the **kiosk's** path and plan, never private arithmetic. Integrity **20/20** (Cyborg **N/A**), **Taint 0** and **no chrome** are read back and warned about, never auto-written. **Chargen never runs twice**: a hero past 1st level, or one stamped complete, reopens **review-only** — for Directors too, because a second pass would drive the advancement dialogs again and double-grant. State lives in `flags.draw-steel-ghostwire.chargen`, so a refresh lands back on the step you left. Copy stays clean: **178** `GHOSTWIRE.Chargen` strings with no Draw Steel / MCDM name-checks and the sheet remaps intact (Ancestry→**People**, Culture→**Background**, Career→**Profession**) — the smoke greps for both. Also on `game.ghostwire.openChargenWizard(actor)`, `module.api.openChargenWizard`, a keybinding, and a new **Ghostwire: Chargen Wizard** macro (macros pack 4 → **5**, rebuilt with Foundry closed). `scripts/voidmark.mjs` gained `VoidmarkChat#seed()` and `openVoidmark({ seed })`. Fixed alongside: `tools/g2-armor-gadget-mods-smoke.mjs` still pinned `module.version === "0.3.100"` by string equality — the one assertion G2's own `module-version.mjs` sweep missed — and now compares numerically. Smoke: `node tools/chargen-wizard-smoke.mjs` (**213** checks). Checklist: `docs/directors/chargen-wizard-smoke-03101.md`. Spike: `docs/spikes/B95-CHARGEN-WIZARD.md`. Open: placeholder-name detection is English-only (it fails in the harmless direction), and the spend catalog shows the first 150 matching rows with the true total printed above the list.
- `0.3.100` — **Wearable armor / shield and gadget mod families published (G2), plus Hexshot's Street bow.** The last two **unpublished** mod host families ship. RAW used to tell the table to treat wearable armor **modSlots as 0** even though every vest, shield, comm and lockpick set in the Gear pack already published slots and a `modFamily` — 41 hosts carrying orphan capacity. **Armor / shields → Published** (Gear master **§2F**, **14 SKUs**) and **Gadgets → Published** (**§1H**, **17 SKUs**), so the **Mods pack goes 47 → 63**. The seven existing armor stubs and eight gadget stubs came up to S8 quality — real `system.project` fields (goal on the trinket ladder **E1 150 · E2 300 · E3 450 · E4 600**, materials ¥ prerequisites, roll characteristics, yield) so fabricating one eats a **Lifestyle project slot** like any other Project — and both families grew a real **Street → Prototype** spread rather than an E1 puddle: armor adds **Brace Struts** ¥1,800, **Insulator Liner** ¥2,200 (lightning immunity 1), **Seal Kit** ¥6,000, **Scanner Null** ¥9,000, **Thermoptic Skin** ¥11,000, **Reactive Plating** ¥24,000 (pick a damage type, immunity 2) and **Denial Field** ¥26,000; gadgets add **Jam Mask** ¥1,600, **Breach Jack** ¥1,900, **Sniffer Head** ¥2,400, **Rad Baffle** ¥2,600, **Habitat Stage** ¥6,800, **Deep Optics** ¥7,500, **Passkey Stack** ¥8,500, **Quantum Link** ¥21,000 and **Ghost Frame** ¥23,000 — every Category-1 family (comms, sensors & optics, mechanical and electronic B&E, survival, Wired) now has a menu at more than one echelon. **The lock holds: no wearable mod adds Stamina.** That number stays on the armor Item, by class and wearer Echelon; the smoke asserts it on the catalog flag, in every Active Effect change key, and in the card text. Only **Thermoptic Skin** and **Deep Optics** carry an AE at all (a Stealth and a Perception edge, shipped **off** so the player switches it on) — the honest pattern **Stealth Weave** already used; every other row is a card the **Director** reads at the table, with no invented Power Roll math. Five **exclusive groups** refuse to double up on one host and Foundry names the clash: *inner liner*, *outer camouflage layer*, *active-denial cell*, *optical stage*, *lock-cracking package*. New `tools/regen-mod-slot-cards.mjs` derives the **Mod slots (N): Fill them with…** paragraph on all **89** host gear cards from the Mods pack, so publishing a SKU never again leaves hand-written @UUID lists behind — it reproduced the 47 untouched weapon cards byte-for-byte before rewriting the 42 that were stale. Table vendors: two new kiosk presets, **Armorer** (§2F) and **Gadgeteer** (§1H), beside the Chop Shop, both auto-stocking on `flags.mod` folder. **Part B** closes the one gap G1 left — Hexshot wants a bow in the light **and** medium slots and the Street band had only the light one, so **Scrap-Bow** (¥300, 5 kinetic, Medium range, Echelon 1 / Street / 1 slot) joins §3F and `kit-grants.mjs` grants both bows; the G1 smoke's documented-gap list is now empty. Rules stay in sync: RAW `10-mods.md` (both tables harvested, every “not yet published” / “modSlots as 0” / “do not invent liners” instruction gone), the Stage 3 Mods chapter, Gear master §1H / §2F / §3F / Gear-Pass Status, the Rulebook journals and the Voidmark index. Mods, gear and rulebook packs rebuilt with Foundry closed. Also fixed a latent repo-wide bug the bump exposed: **18 smokes compared `module.version` as a string**, so crossing 0.3.99 → 0.3.100 read `"1" < "9"` and turned them all red at once — they now share `tools/lib/module-version.mjs`. Smoke: `node tools/g2-armor-gadget-mods-smoke.mjs` (792 checks) and `node tools/kit-grants-smoke.mjs` (478 checks). Checklist: `docs/directors/g2-armor-gadget-mods-smoke-03100.md`. Open: the 16 new SKUs use core Foundry placeholder icons until plates land (`tools/apply-gear-token-art.mjs`), and the print manuscript reprints on the N4 PDF pass.
- `0.3.99` — **Kit chargen street-band grants (G1), and Static Crow drops Mark.** A Kit names a *category* and the hero must **own** something in it or the Kit is inert — so the free starting Kit now arrives with the street-band object that makes it live on day one. `scripts/kit-grants.mjs` maps **all 28 Kits** to street SKUs: one armor piece when the Kit wants armor, **Riot Shield** when it wants a shield, and one weapon per entry in the Kit's own `system.equipment.weapon`. Granted Items are stamped `flags.draw-steel-ghostwire.kitStreetGrant`, the actor keeps a `kitStreetGrants` ledger, and the gate is narrow: **level 1**, and this Kit `_dsid` has never been granted on this actor. **Merc** needs no special case — the Operator dual-Kit Origin grants two kit Items at 1st level, so both packages land. A Kit taken **later** grants nothing: the hero buys the new category with ¥ + Availability, and the **ownership rule still bites** — sell the object and the Kit goes inert. **Mods and chrome never auto-grant**, nothing above **Street** is ever handed over, a SKU the hero already owns is skipped rather than duplicated, and there is deliberately **no back-fill sweep** over existing heroes. Kits that need no object get none: **unarmed** Kits (Brawler, Mantis) fight with fists by rule, and the three Hacker **deck-Kits** and the **Rigger's Harness** *are* the host — no deck or RCC on top. Pouring that floor needed gear: the Street column had **no** heavy, polearm, whip, ensnaring, medium-melee or medium-band sidearm weapon, and `polearm` did not exist at **any** Availability — six Kits had nothing the Economy could hand them. **Street weapons 7 → 14** with **Slugger** (¥300 heavy revolver, the one street handgun in the Medium band), **Pipe Rifle** (¥300 single-shot marksman rifle), **Scrap Cleaver** (¥280 medium melee), **Slab-Hammer** (¥300 demolition maul), **Scaffold Pike** (¥250 rebar pike-staff), **Chain Lash** (¥200 chain whip) and **Weighted Net** (¥120, no damage line, Restraining) — all **Echelon 1 / Street / 1 mod slot**, damage at the band floor per the Damage-Bridge, firewall intact. Alongside it, **Static Crow drops its Mark tag** (now **EW, Jump-In-Capable, Wired**): the kite paints a lane as an artefact of its EW suite, not as a scout designation, so it no longer grants **Street Eye**. Michael's call — drop Mark rather than widen the gate. The gate is unchanged (Personal or Light **Air** frame tagged **Recon** / **Mark** / **Decoy**) and **Spotter** (E1) and **Phantom** (E4) still qualify; v1 frames **9 → 8**. Rules stay in sync: RAW `08-kits-gear-wealth.md` and `23-machines.md`, the Drones chapter, Gear master §3A / §3B / §3D / §3F, the Rulebook journals, the Voidmark index and the print manuscript. Gear, vehicles and rulebook packs rebuilt with Foundry closed. Smokes: `node tools/kit-grants-smoke.mjs` (467 checks) and `node tools/street-eye-smoke.mjs`. Table: `docs/directors/kit-street-band-grants.md`. Checklist: `docs/directors/kit-grants-smoke-0399.md`. Open: no medium bow/crossbow at Street (Hexshot's second slot, a G2/S2 line), and the seven new SKUs use core Foundry placeholder icons until plates land (`tools/apply-gear-token-art.mjs`).
- `0.3.98` — **Vehicle / drone / mods build-out (S8).** The Machines layer gets a real catalog. **Crewed platforms 40 → 47** (E1 20 / E2 10 / E3 9 / E4 8) with **Dock Tug** (the Street-band sealed hull, and the first E1 Space chassis), **Trauma Barge** (LAZ flood-canal ambulance), **Lane Bus** (Flats shaft shuttle — a crew or a squad in plain sight), **Gale-Runner** (E3 pursuit VTOL), **Ash-Crawler** (sealed expedition tractor), **Longshore** (E4 quiet deep freighter) and **Black Ledger** (E4 airborne command-and-EW post). **Drones 36 → 40** with **Kiln-Beetle** (armored repair), **Static Crow** (EW + designator), **Tide-Wraith** (apex aquadrone) and **Second Face** (apex anthro). **Vehicle/drone mods 14 → 24**: the armor and weaponry ladders are unchanged one-at-a-time ladders, and the “other” menu grows to **16** stacking rows — Burner Plates ¥500, Lane Skirt ¥700, Spool Rig ¥900, Drop Harness ¥1,800, Signal Mule ¥2,200, Ghost Rein ¥3,400, Deep Shell ¥5,500, Spoof Cowl ¥6,500, Kick Drive ¥17,000, Storm Lattice ¥19,000 — covering mobility, cargo, insertion, link/EW, environment and sensors. **¥ on every SKU:** the last hole is closed — **Nox’s Trash Freighter** was ¥0 and now prices at **¥2,800** replacement while keeping its **Plot** tag, so it still never reaches the Vehicle Lot shelf. **Fabricate is the stock Draw Steel Project:** every chassis and machine mod now carries real `system.project` fields — goal on the system’s trinket ladder (**E1 150 · E2 300 · E3 450 · E4 600**), prerequisites, roll characteristics (Repair → Might or Reason, Electronics → Reason or Intuition) and yield — so building one eats a **Lifestyle project slot** like any other Project and needs no parallel tracker. Fabricating yields the part; installing it is still its own §Craft Project. **Ghost Rein** buffers Jump-In biofeedback and **never** grants Jump-In — that stays Wrench-only behind Jump-In Capable or a Rigger Cocoon. **Table vendors restocked:** a new **Mods** kiosk preset (*Chop Shop*) sells all **47** mod SKUs, and the existing **Drone Vendor** / **Vehicle Lot** presets pick up every new chassis with no listing UUID edited — all three match on catalog flags, so future SKUs auto-stock. Dual Item + Deploy Actor is unchanged. Rules stay in sync: `docs/raw/23-machines.md`, `docs/raw/10-mods.md`, Gear master Cat 5 / §5F, the Stage 3 chapters, the Rulebook journals, the Voidmark index and the print manuscript. Vehicles, mods and rulebook packs rebuilt with Foundry closed. Smoke: `node tools/s8-machines-smoke.mjs` (72 checks) and `node tools/kiosk-smoke.mjs`. Checklist: `docs/directors/s8-machines-smoke-0398.md`. Open art gap: the eleven new chassis use core Foundry placeholder icons until plates land (`tools/apply-machine-token-art.mjs`).
- `0.3.96` — **Black Market sell (F9).** The other half of the ¥ economy: `scripts/black-market.mjs` turns owned gear back into cash. **List price is the catalog ¥** — `catalogPrice()` from `kiosk.mjs`, the same gear / chrome / matrix / mod / vehicle / focus `.price` flag the kiosk charges — and the street pays **50% of it**, floored. A SKU with **no catalog ¥ refuses the sale**; nothing invents a price. An optional **haggle** is one stock Draw Steel Power Roll on Presence (Intuition or Reason if the seller argues a different angle) and moves the percentage only: **low (tier 1) pays 50%** — the roll bought nothing — **middle (tier 2) 55%**, **high (tier 3) 60%**. Dismissing the roll **cancels the sale** rather than quietly fencing at 50%. Stacks are handled: a line's list price is catalog ¥ × the quantity sold, so selling 3 of 5 grenades pays for three and leaves two. Items go first and the ¥ lands second — a failed purse write **puts every Item back**, so a sale is never half-done, and the only path written is still `system.hero.wealth`. Sellers are the hero's **owner** or the **Director** (who may run it for a targeted or selected hero, the Director Taint +1 collect shared with F6), and an Item is only sellable when it sits on that hero and the user can delete it. Doors: right-click a priced Item on the hero sheet → **Black Market: Sell…**, a **Token** scene-control tool, a token-HUD sack, an unrestricted keybinding, the **Black Market: Sell** Ghostwire Macro (`gwBlackMktSell01`), and `blackMarketPrompt` / `blackMarketSellPrompt` / `executeSale` / `sellItem` on `module.api` and `game.ghostwire`. Complements the Gear kiosk (buy) and F6 Pay / Spend. Macros pack rebuilt with Foundry closed (`node tools/build-packs.mjs macros`). Smoke: `node tools/black-market-smoke.mjs`. Checklist: `docs/directors/black-market-smoke-0396.md`.
- `0.3.95` — **Ritual Seal artifacts (F8) and the Director's purse (F6).** Sealing a Ritual Working now leaves something on the table. A **held** (tier 2) or **clean** (tier 3) seal stamps a **Ritual Effect** — `flags.draw-steel-ghostwire.ritualEffect` carrying Working, Formula, family, Magnitude, Ritual Leader, `sealedAt`, expires, scope, tier and outcome — onto an artifact picked by the Formula's **family** rather than by rite name, so one Ward card working means every Ward card works: **Ward / Threshold** stand a neutral, turn-less **scene marker** Actor and Token one square from the Ritual Leader, filed in a new **Ritual Effects** folder, its biography printing the whole block; **Calling** goes down the existing `veil-summons.mjs` path when the card names a template and otherwise rides the Leader as an Active Effect with a chat line handing the placing to the Director; **Artifice** folds into a linked Item on the Leader; **Reach** leaves an Active Effect and no token; **Unmaking** leaves the chat confirmation only. A **hung (tier 1) seal leaves nothing**, and the Director's stage-4 **Reset** or **Abandon Working** clears the artifact off every Scene. Alongside it, **Director: Pay Hero / Spend Hero** — `scripts/director-wealth.mjs` — gives the Director the counterpart to the Gear kiosk: target or select heroes, answer direction / amount / reason, and ¥ moves on `system.hero.wealth` with a chat card naming who, which way, how much, why and **before → after**. Pay always credits; **spend refuses what the purse cannot cover** and writes nothing, so ¥ never goes negative. Two GM scene-control tools, a GM token-HUD ¥ button, a Director-restricted keybinding, two **Ghostwire Macros** (`gwDirPayHero0001`, `gwDirSpendHero01`) and `directorPayHero` / `directorSpendHero` / `directorAdjustWealth` / `directorWealthPrompt` on `module.api` and `game.ghostwire`. The resource firewall is unchanged: a seal artifact never spends Essence, Conviction or Resonance, and the only ¥ in the ritual stack is still Pay Components. Macros pack rebuilt with Foundry closed (`node tools/build-packs.mjs macros`). Smokes: `node tools/ritual-seal-smoke.mjs`, `node tools/director-wealth-smoke.mjs`. Checklists: `docs/directors/ritual-seal-smoke-0395.md`, `docs/directors/director-wealth-smoke-0395.md`.
- `0.3.94` — **Magical Societies contact Actors (F7).** The three locked society contacts are now playable NPC sheets in a new **Magical Societies** folder (`gwBestiaryMagSoc`, sort 175000) under **Ghostwire Bestiary**, beside Reach Streets and Reach Critters: **Edda Marr** (`gwBesEddaMarr000`) — Senior Examiner of the Measure Collegium out of **Datum House**, a **Corran Elementalist who casts**: The True Measure, Measurements Over Testimony, the Director-facing pitfall *The Institution That Trained Her*, the signature cast **Plumb Drop** and the Malice-fed area **Bring It to True**. **Tavi Sorn** (`gwBesTaviSorn000`) — Wickkeeper kitchen organizer at **the Last Kettle**, **Pure Human with no casting ability at all**: Remembers Every Name, The Night Window, **No Casting, No Claim** (she cannot ward, cleanse, bless, grant Conviction or certify anyone holy), the social maneuver *Until Morning*, and one reluctant strike. **Daska Venn** (`gwBesDaskaVenn00`) — Ash Survey Field Coordinator at **the Cinder Yard**, a **full-conversion Cyborg who cannot cast**: **Arcane Severance** stated on the sheet and never waived by membership, Sealed Frame, The Dirty Return, *A Clean Report Is Not a Clean Site*, and the extraction strike **Winch Line**. Each sheet carries a Director hook with what she can and cannot do, negotiation motivation/pitfall, and an `@UUID` link to her 0.3.93 lore journal. Art is Michael's 2026-09-22 drop under `assets/tokens/bestiary/magical-societies/`, one plate per contact on both portrait and prototype token (Has Vision ON, neutral disposition); all three ship a **Wire Kit** per the humanoid Connect pass. The folder is generated by `node tools/gen-magical-societies-cast.mjs` and regenerates byte for byte. No new source, prices, ward strengths or casting permissions. Bestiary pack rebuilt with Foundry closed (`node tools/build-packs.mjs bestiary`). Smoke: `node tools/magical-societies-cast-smoke.mjs`; Foundry checklist `docs/directors/magical-societies-contacts-smoke-0394.md`.
- `0.3.93` — **Magical societies in Foundry, ¥250 on every pregen, and a safe pregen regen (F4).** Three locked societies ship as lore journals in a new **Magical Societies** folder (`gwLorePackSoc000`) in **Ghostwire Lore**: **The Measure Collegium** (The True Measure · Datum House · Senior Examiner Edda Marr), **The Wickkeepers** (The Sheltered Wick · The Last Kettle · Tavi Sorn) and **The Ash Survey** (The Held Fault · The Cinder Yard · Field Coordinator Daska Venn). Sources are `docs/manuscript/01-lore/L6-measure-collegium.md`, `L7-wickkeepers.md` and `L8-ash-survey.md`, built by `node tools/lore-to-journals.mjs`; each journal opens on its symbol and carries an HQ plate plus a read-aloud on the headquarters page. Six new `ART-PLACEMENT.yml` slots put the shipped WebP under `assets/factions/magical-societies/` on the matching heading — **0 art gaps**. The **Lore Index** picks up the new folder automatically. Every pregen now starts with **¥250** spending money: `system.hero.wealth` is `250` for all **seven** heroes, set from `START_WEALTH` in `tools/pregens-to-actors.mjs` so a regen keeps it — the kiosk (`scripts/kiosk.mjs`) spends this field against Gear prices, so it is a real balance, not a wealth tier. **F4** lands with it: portraits, Changer form art, class level, Taint / Corruption History, Vira's Fabricator's Bench, Sabbat's installed Whiteout and Ward the Room learned on Kaïs / Vessa / Sabbat all come from the ROSTER, `loadouts.json` or the new `docs/masters/pregens/post-patches.json`, so `node tools/pregens-to-actors.mjs` reproduces `src/packs/pregens/` byte for byte. Lore and pregens packs rebuilt with Foundry closed. Smoke: `node tools/pregen-regen-smoke.mjs`; Foundry checklist `docs/directors/magical-societies-foundry-smoke-0393.md`.
- `0.3.92` — **Plot & Run is a top-level Gear folder.** `gwGearPlot000000` moves out from under **General & Lifestyle** to sit beside Armor / Street Consumables / Weapons in **Ghostwire Gear**, sorted last (20000). Source moves `src/packs/gear/general/plot/` → `src/packs/gear/plot/`; the folder and its three Deadhead plot Items keep their ids (Mama’s Deadhead Job Stick, ARG Courier Capsule, Gold Line Aerial Recon). The lang entry is re-keyed `GHOSTWIRE.Gear.Folders.GeneralPlot` → `…Folders.PlotRun` and now reads **Plot & Run** (was “Plot & Run Hooks”). Gear pack rebuilt with Foundry closed (`node tools/build-packs.mjs gear`). Smoke: `node tools/deadhead-director-smoke.mjs`.
- `0.3.91` — **Ritual Working applet.** The five stages of a Working — **Study → Components → Sanctum → Seal → Payoff** — in one panel (`scripts/ritual-working.mjs`), openable by **every player** from the token scene controls, a keybinding, `game.ghostwire.openRitualWorking()`, or right-click a Formula → **Start Ritual Working**. Whoever owns the Formula Item and picks it is the **Ritual Leader**: every Project roll, the sealing Power Roll and the components ¥ land on that hero. Stages that are Projects are **stock Draw Steel Project Items** on the leader — Study at goal Magnitude × 2, a built sanctum at Magnitude × 3 — rolled with the system's own low1 / middle2 / high3 dialog and eating Lifestyle project slots like any other Project; there is no parallel tracker. Components read the card's ¥ totals off `flags.draw-steel-ghostwire.ritual.componentsTotal` (Magnitude ladders and component paths both), and **Pay** debits the leader's `system.hero.wealth` through the kiosk's helpers. Magnitude 4–5 forces a built sanctum (temporary caps at rating 3). **Seal Ritual** makes one Power Roll on the tradition characteristic (Persona for a Street Priest, Logic for an Elementalist or Technomancer; overridable) and posts low / middle / high — a low result is the Director's hung ritual. Sealing stays locked until the Formula is **learned** (0.3.89), the components are bought and the sanctum is ready; finishing the Study Project offers the learned stamp. Chat cards for started / paid / sanctum / sealed / payoff carry the Veil-pressure leak band and the spirit-attention tick. A Working never spends Essence, Conviction or Resonance. Scripts, template and styles only; no pack rebuild. Smoke: `node tools/ritual-working-smoke.mjs`; Foundry checklist `docs/directors/ritual-working-applet-smoke-0391.md`.
- `0.3.90` — **Constructs & Pets action-economy FAQ (S7).** New RAW chapter `docs/raw/28-constructs-pets-faq.md` — the player-facing answer to *when does my sprite get a turn?* Fourteen short Q&A entries covering the shared **Minor (L1–3) extension / Intermediate (L4–7) commanded / Advanced (L8–10) independent** band ladder for Technomancer **sprites** and Hacker **Agents**, the command-maneuver tax at levels 4–7, caps (2 → 3 at 5th → 4 at 8th; Sprite-Weaver 3 from 1st), the Stamina lock (pets use **Stamina**; extension spirits have no pool), decompile triggers, Street Priest **extension vs. independent** by Invoke the Pact result, Elementalist extension/independent by Persistent investment, Agents-are-not-sprites, Linked refuses a compile, biofeedback buffering, and **Lock A** (a construct's scene token is a roster anchor — no meat-space attacks unless an ability bridges). Restates existing locks only — **no new numbers**. Cross-linked from `17`, `18`, `19`, `20`, and Veil §C3; wired into the Rulebook pack (Ghostwire Systems), the print manuscript (Appendix ch30, locked TOC 1–29 untouched), and reindexed for Voidmark as 18 retrievable chunks. Checklist `docs/directors/constructs-pets-faq-smoke-0390.md`. No PDF.
- `0.3.89` — **Formula study at the table.** Right-click a Ritual Formula in a hero's inventory (flag `draw-steel-ghostwire.ritual.formula`, or the `RitualFormula` gear tag) → **Mark learned**, which sets `ritual.learned` and posts a chat card naming the student and the card's family / Magnitude / leaders. The Director gets **Mark unlearned** to take it back. An owned Formula's sheet now says whether it is learned — owning a Formula still never lets you cast. Scripts only (`scripts/rituals.mjs`); no pack rebuild. Smoke: `node tools/ritual-learn-smoke.mjs`.
- `0.3.88` — **Ritual Workings: full card catalog + Ritual Formulas.** The Veil (`docs/raw/22-the-veil.md`) now carries the one-chapter Ritual Workings shape: shared rules once at the top (resource firewall — rituals never spend Essence / Conviction / Resonance; Magnitude spelled out; study = study time **and** Project progress Magnitude × 2; temporary sanctum cap 3; low / middle / high sealing results), then **46 Working cards** split across Rulebook pages (Card Index · General · Elementalist · Street Priest · Technomancer Wire-rites). Every card prints a priced component table (playtest ¥ estimates inside the Magnitude bands; fetch reagents `¥ —`; optional reagents out of the total). **Glyph Cage is renamed Data Ward.** New **Seal the Flat** (Magnitude 2 General Ward, ¥180) is the full Project prototype — study 4 / sanctum 6 / sealing 4 — with a story walkthrough. New Ghostwire Gear → General → **Ritual Formulas** folder: 46 Formula Items (`gwRit…` ids, tag `RitualFormula`, description = the full card, `flags.draw-steel-ghostwire.ritual`) generated from RAW by `node tools/ritual-formulas-to-items.mjs`. Formula Items are rare esoterica — on no kiosk shelf. Kaes, Vessa and Sabbat start with **Ward the Room** learned (`loadouts.json` `rituals`). Journals, manuscript and VOIDMARK regenerated. Smoke: `node tools/ritual-formulas-smoke.mjs`; Foundry checklist `docs/directors/ritual-foundry-smoke-0388.md`. No PDF.
- `0.3.87` — **Deadhead Foundry push.** The official Ghostwire session package lands: **Deadhead — Session Chapters** (`gwDhSessionChap0`) in Ghostwire Runs → Deadhead, one page per Foundry Scene (Mama's Club → Flats Transit → Rack & Rest → Freighter Meet → Gold Line → Nightjar Market) stamped from `DEADHEAD-SESSION-CHAPTERS.md`. READ ALOUD — Meatspace / Wired boxes and Beat headings stay player-visible; Director lines, If pressed, dials, seats, and Director Notes sit in Foundry secret blocks, and the Gold Line car-by-car chamber cards are a hard GM-only page. **Deadhead — Director** (`gwDeadheadDirJrn`) is regenerated to Michael's locks: opposition **5 security + 1 worker drone** (2 Enforcers L1 · drone L2, Integrity 24 · Officer mid booth · Officer-as-guard with the capsule · Lieutenant R3 cab), wafer in the **second-to-last** car, and the exchange is the **Nightjar Market** Scene — no 4-Officer nest, no R1 courier, no club return, no Handoff Beat. SoR / cargo remap / car-by-car / Map Notes aligned. Eight **Ghostwire Runs — Deadhead Actors**: Nightjar buyers **Iona Vale** / **Rhen Calder** / **Nim** (L1 platoon contacts per the push Appendix) and Rack & Rest Faces **Vesper Drift** / **Tam Kade** / **Sera Nix** / **Cousin Vell** / **Juno Halve** (L1 ambience band), all with Wire Kit and Michael's tokens under `assets/tokens/deadhead/` (1254² PNG + 1024² WebP); proposed 15-char ids padded to Foundry's 16. Nox portrait rides on the freighter Actor. **Nightjar Market** world Scene is a create-once inject (**Scenes → Deadhead**, nav after Gold Line, plate 1579×915, grid 63 px) that never rewrites an existing Nightjar Scene and has no force path. Gold Line untouched — no `{ force: true }`. Flats Transit journal + transit lore/RAW stubs ride along. No ritual / Street Magic. Generators: `node tools/deadhead-to-journals.mjs`, `node tools/gen-deadhead-cast.mjs`. Smoke: `node tools/deadhead-session-smoke.mjs`. No PDF.
- `0.3.86` — **Victories / respite / ¥ pacing default.** RAW `24` default is Victory→XP at full respite (thresholds 5 XP for levels 1→5, 8 XP for 5→10; dial 6/9 or 4/6). Live Victories still fuel heroic resources; bank 1:1 to XP and reset at full respite; level when XP ≥ threshold and keep remainder — **no** auto-level on job-complete (optional fast-table alternate retained). `26` distinguishes full respite vs mid-job short stop (Lifestyle / payday / XP bank only on full respite). `08` payday is integrated `U + G + ~25%` by level band; L6+ half favor OK. Quiet Floor campaign worked example in `docs/directors/campaigns/`. Journals + VOIDMARK regenerated. Smoke: `node tools/voidmark-smoke.mjs`. No PDF.
- `0.3.84` — **Mama's Club generic patrons, fixed and reproducible.** Follow-up to 0.3.83. The three unnamed **Club Patron** Actors (male, female Corran, Revenant) were hand-written outside the generator and shipped with four defects: all three sat on `sort` 510 (colliding with Torque “Gasket” Venn, so the sidebar order shuffled), carried Stamina 15 on an L1 minion (the street minion band is 4–6), carried a stray lowercase `freestrike` key beside `freeStrike`, and wore Dren Holt's barback kit — **Keg Haul** (explicitly “keys to the service corridors”) and **Empty Bottle** — on paying customers. All three are now emitted by `tools/gen-mama-club-cast.mjs` like the named thirteen, on the plain L1 minion band (Stamina 4, EV 3), sorted 600/610/620 behind the named cast, with a patron-appropriate kit: **Face in the Crowd** (**Cold Regular** for the Revenant) plus **Improvised Swing**. The Revenant patron keeps `[humanoid, undead]` and picks up corruption / poison immunity 1. As of 0.3.85 every humanoid in the club also ships Wire Kit (Soft Trace stays the info broker). The smoke now guards all four regressions plus generator reproducibility: every Actor in the folder must be emitted by the generator. Smoke: `node tools/mama-club-cast-smoke.mjs`. No PDF.
- `0.3.83` — **Mama's Club floor cast.** Sixteen Actors under Ghostwire Bestiary → Reach Streets → **Mama's Club** — the staff and patrons of Mama Cassavir's club, as plain-English Actors with a written biography and a Director plot hook on every sheet. Staff: **Vexa “Brick” Mol** (lead bartender), **Unit-7 “Halo”** (stair guard), **Static Veil** — **Nyx Vale** / **Jax Coil** / **Rook Anvil** (the house band), **Pip “Tray” Riss** (floor wait), **Dren Holt** (barback), **Sable Quen** (VIP wait). Regulars: **Madame Quill** (Revenant), **Torque “Gasket” Venn**, **Luma Glass**, **Brother Ash**, **Kira “Soft Trace” Bell**. Plus three unnamed **generic patrons** (male, female Corran, Revenant) as crowd texture — one-line bios, filler-seat hooks, plain minion math. Mook math mirrors the existing street bands — L1 minion / L2 platoon, one L2 horde — and minions keep the With Captain rider. **Soft Trace Bell is the only Wired body on the floor:** she ships the **Wire Kit** (`wire-kit-matrix-verbs`, `wired.connectInterface`); the other fifteen are meat and cannot Connect, which is what keeps the club a safe room. Ancestry (Goliar, Elvani, Corran, Rat-Changer, Revenant) lives in the prose and the bestiary flags, not in `system.monster.keywords`. Token Has Vision on, disposition neutral. Michael's club plates ship as `assets/tokens/bestiary/mama-club/<slug>.{png,webp}` (1254² PNG master beside the 1024² WebP Foundry renders), one per Actor. Mama Cassavir herself is untouched. Generator: `node tools/gen-mama-club-cast.mjs`. Smoke: `node tools/mama-club-cast-smoke.mjs`. No Gold Line `{ force: true }`, no PDF.
- `0.3.82` — **Corp & Security can Connect.** Playtest fix: every Actor in the Ghostwire Bestiary **Corp & Security** folder now embeds **Wire Kit** (`wire-kit-matrix-verbs`, `wired.connectInterface`) — the Director stamp that *is* a Connect interface, so no separate commlink or deck is needed. Twelve Actors stamped: Corp Security Officer, Corp Enforcer, Corp Netrunner, Contract Enforcer, Response Lieutenant, Warden Krael, and the six Ironclad ranks. Corp Netrunner keeps its existing decking gear. The embedded kit now reads **Wire Kit** on the sheet (new `GHOSTWIRE.Matrix.Items.WireKit.ShortName`) instead of the long `Wire Kit — Matrix Verbs` label, on AEQ/LAZ too. Critters, Veil, Wilds, and meat-only street punks stay unstamped. Smoke: `node tools/corp-security-wire-smoke.mjs`. No PDF.
- `0.3.81` — **AEQ / LAZ conglomerate NPCs.** Four Ghostwire Bestiary Actors under Corp & Security → **Aequitas Mandate** / **Lazarus Extract**: Mandate Trooper, Mandate Sergeant, Extract Medic, Extract Chief Medic. Michael circular plates (`aequitas-mandate-officer`, `lazarus-combat-medic`) stamp both ranks of each corp. LE kit + HUD/radio chrome + **Wire Kit** (starts Disconnected); Lazarus trauma bag / Stabilize / stim / patch + Medical HUD + Wire Kit. Faction flags `aeq` / `laz`. Token Has Vision on. Smoke: `node tools/aeq-laz-npc-smoke.mjs`. No Gold Line `{ force: true }`, no PDF.
- `0.3.80` — **Conglomerate service vehicles (AEQ / LAZ).** Four dual-model SKUs: **Seal Cruiser** and **Writ VTOL** (Aequitas Mandate — Mandate cruiser / Council badge air) plus **White Door** and **Crash Angel** (Lazarus Extract — Laz ambulance / Laz chopper). Treasure Items in Ghostwire Vehicles & Drones (E1 Professional, kiosk Vehicles shelf) + named Summons Actors with Michael top-down plates (`assets/tokens/vehicles/{seal-cruiser,writ-vtol,white-door,crash-angel}.{png,webp}`). Deploy bands: ground → `vehicle-car`, VTOLs → `vehicle-air`. Wire Kit, start Disconnected. RAW / gear / director inventory 35→39. Smoke: `node tools/service-vehicles-smoke.mjs`. Twelve Conglomerates lore stays a separate PR — names cross-link only. No Gold Line `{ force: true }`, no PDF.
- `0.3.79` — **Twelve Conglomerates: Aequitas Mandate (AEQ) + Lazarus Extract (LAZ).** Tickers locked. AEQ sells **only to the Council** (Congress / Chair / pooled authority — never a private party, runner, gang, or a single mega acting alone). LAZ trauma extract is **bundled into Lifestyle upkeep** (`26`) — Squatter none / Low Basic / Middle Standard / High Priority / Elite White Door. L1 full profiles; glossary slang; Foundry lore journals; Host + brand plates shipped: `brand-aeq` / `brand-laz` and `node-host-aeq` / `node-host-laz` (PNG 1254² + 1024² WebP). Charter Age may still say **the Ten**; current play uses **the Twelve**. Kestrel stays the seatless thirteenth-chair climber. Same pass: Pet Stamina lock (sprites / Agents / independent spirits; extension spirits no separate pool) + Construct Wire visibility + Constructs Console Lock A (`21`/`22`). VOIDMARK index rebuilt. No PDF, no new bestiary Actors. Smoke: `node tools/twelve-conglomerates-smoke.mjs`, `node tools/ten-conglomerates-art-smoke.mjs`. Spike: `docs/spikes/B121-TWELVE-CONGLOMERATES.md`. **Version note:** rebased onto main after Constructs Console **0.3.78** (PR **#61**); lands as **0.3.79**.
- `0.3.78` — **Wired Console Constructs (B121).** Separate **Constructs** section for compiled Technomancer sprites and Hacker Agents. Name, archetype, Stamina, hybrid band, compiler, optional Wire-face chip — **no** construct edges on the Connections/Nodes graph. Lock A: scene token is a meat-side roster anchor; Wire play is the Console; meat actions off unless an ability bridges. **Same-scene Overlay / Jacked In compilers see each other’s constructs without Scan** (Linked does not; pan / Command / Decompile stay owner + Director). Smoke: `node tools/b121-console-constructs-smoke.mjs`. No Gold Line `{ force: true }`, no PDF. Spike: `docs/spikes/B121-CONSOLE-CONSTRUCTS.md`.
- `0.3.77` — **Kiosk presets: Vehicles / Decks / Programs / Ammo.** Place-kiosk type picker and Restock gain four shelves beside Food / Medical / Tools / Armor / Weapons / Drones (Drones unchanged). **Vehicles** = vehicles pack crewed platforms (`flags.vehicle` and not `drone`; Lane-Hopper, Star-Chopper, Bulldog, …). **Decks** = matrix Cat 4A cyberdecks. **Programs** = buyable deck software — Cat 4B suites **and** Cat 4C attack payloads on one shelf (autosofts and Hacker class Program abilities stay off). **Ammo** = Gear › General › Ammunition magazines / one-shot rounds (not machine Ammo Bin mods). Same default kiosk token art. Smoke: `node tools/kiosk-smoke.mjs`. No Gold Line `{ force: true }`, no PDF.
- `0.3.76` — **Compile Agent sheet Use places the Agent token.** 0.3.72 only spawned from the Item-sheet Compile button / row menu, so Abilities-tab **Use** ran the stock Draw Steel power roll with no picker and no token. Wrap `AbilityModel#use`: under cap + Overlay/Jacked In opens Probe/Spike/Daemon/Watchdog then places the token (`skipSpend` so Bandwidth is not charged twice); at cap, Use is command-only; Linked/Disconnected notify and refuse. **Use Decompile Agent** picks one Agent or the roster. Compile Sprite and Matrix Verbs unchanged. Rebased onto main after 0.3.73 icons and 0.3.75 Mule-Bot. Smoke: `node tools/hacker-agents-smoke.mjs`. No Gold Line `{ force: true }`, no PDF.
- `0.3.75` — **Mule-Bot Deploy path verified (dual Item + Actor).** Draw Steel **treasure** type on Ghostwire Vehicles & Drones is **intentional** — every drone/vehicle SKU is an inventory Item (ownership / ¥ / mods). Deploy stamps a linked Actor from the Summons band template; Recall deletes the Actor and the Item stays. Mule-Bot flags (`drone: true`, Ground (drone), Scale Vehicle) already mapped to `drone-medium` like Stinger / Warhound. Named Summons **Mule-Bot** Actor is optional Director placement for an unowned mule (Wire Kit, industrial hauler plate) — not a conversion off treasure. Deploy still uses `machine-drone-medium` so other drones are unchanged. Smoke: `node tools/mulebot-actor-smoke.mjs`. No Gold Line `{ force: true }`, no PDF.
- `0.3.73` — **Compile Agent / Decompile Agent sheet icons.** Michael smoke 0.3.72: those abilities had no image (Foundry core `icons/commodities/tech/circuit-board*.webp` is not on disk). Module SVGs at `assets/icons/abilities/compile-agent.svg` (hex + spark spawn) and `decompile-agent.svg` (hex + dismiss X), same 128² Ghostwire style as 0.3.66 consumable icons. Stamped on class pack items, Hacker L1 Agents grant, and Kessic embeds. Smoke: `node tools/hacker-agents-smoke.mjs`. No Gold Line `{ force: true }`, no PDF.
- `0.3.72` — **Hacker Agents (B120): sprite-parity pets.** Compile Agent / Decompile Agent on the Hacker class. 12 summon Actors (Probe / Spike / Daemon / Watchdog × minor/intermediate/advanced) under Summons › Agents — not sprite SKUs. 3 Bandwidth in combat; Overlay or Jacked In required (Linked refuses). Cap 2 → 3@5 → 4@8. Sheet button + context menu mirror Compile Sprite. Placeholder token art. Smoke: `node tools/hacker-agents-smoke.mjs`. No Gold Line `{ force: true }`, no PDF. Spike: `docs/spikes/B120-HACKER-AGENTS.md`.
- `0.3.71` — **Vehicle/drone Armor = Stamina** (Michael lock 2026-09-20). No machine armor ratings / DR. Four Armor kits (Scrap-Weld +6 / Plate-Up +12 / Combat Plate +18 / Aegis Kit +27) and four Weaponry kits (Gun Rack → Twin Mount → Turret Ring → Heavy Hardpoint). One armor kit and one weaponry kit at a time. Hosts `vehicle` + `drone`. **Install on a Deployed machine actually applies:** armor uses the hero-armor AE path (`system.stamina.bonuses.treasure`) and raises/clamps current Stamina; toggle-off and uninstall reverse it. Weaponry stamps Gunnery hardpoint flags + a sheet AE. Other §5F mods stamp flags + a sheet AE (Director applies those edges/banes at the table). Plate-Up kept as E2 (no Handling cost). Ammo Bin feeds any E1–E4 Weaponry kit. RAW + gear master + journals + VOIDMARK. No Gold Line `{ force: true }`, no PDF.
- `0.3.69` — **Vehicle lore lock (Michael 2026-09-20) + Lane-Hopper POV + Star-Chopper hover-bike + Bulldog hauler.** Most vehicles are **electric**. Street / POV = **light electric hovercraft** with altitude limiters **~25–50 ft** (not sky cars). Ground (tires/treads) = **almost always heavy lifters, haulers, and big equipment** — not everyday street cars. **VTOL** = more expensive but common (corp, transit, well-funded runners). **Lane-Hopper** (Open Stripe) is the 4-seat street hovercar yes-example — Item + placeable Actor (token 2×3) + Michael’s top-down plate. **Star-Chopper** (Long-Fork) is the tandem 2-seat stretch-fork hover-bike (token 1×3; not the rifle named Chopper). **Bulldog** is the street cargo van (Ground-hauler, tires, placeholder art, token 2×4). Journals + VOIDMARK rebuilt. No Gold Line `{ force: true }`, no PDF. Spike: `docs/spikes/VEHICLE-LORE-LOCK.md`.
- `0.3.68` — **Wire Kit is a Connect interface; pack drones are Wire-ready; Wrench drone control (RCC or Rigger’s Harness) counts as Connect.** Director-stamped NPCs/drones with **Wire Kit — Matrix Verbs** on Features could open the Wired Console but Connect stayed disabled (“Need a comlink, deck, datajack, or trodes — or be a Technomancer.”). `itemIsConnectInterface` only accepted `wired.connectInterface` / matrix role deck|rcc|interface / Technomancer; pack item had `kind: "wire-kit"` only. Treat `kind === "wire-kit"` / `_dsid === "wire-kit-matrix-verbs"` as interface; stamp `connectInterface` on the pack JSON and HUD grant. **Pack drone templates** (`machine-drone-micro|small|medium`) **and pack vehicle templates** (`machine-vehicle-bike|car|heavy|air|water|space`) embed Wire Kit so Deploy/import is Wire-ready — still Disconnected until Connect (not auto-Overlay). World ready stamps missing kits onto `kind: "drone"` and `kind: "vehicle"` actors. **Wrench drone control (RCC or Rigger’s Harness) counts as a Connect interface** (Harness ≡ deck). Fabricator’s Bench and Field Chassis are not interfaces. Existing world copies (kind only) Connect without a separate commlink. **Mule-Bot / generic Drone (Medium)** use Michael’s circular yellow cargo-hauler plate (`assets/tokens/drones/mule-bot.{png,webp}`). RAW `21-the-wire.md` **Wire interface (Connect)** section, Foundry journals, and the VOIDMARK index stay in sync (Wire Kit / Rigger’s Harness / pack drones and vehicles). Verbs stay off the sheet. No Gold Line `{ force: true }`, no PDF.
- `0.3.67` — **Hero and NPC tokens always have Has Vision on.** World create, pack import, and compendium drag set `prototypeToken.sight.enabled: true` (and the placed token) for Actor types **hero** and **npc**. Existing range / angle / visionMode are left alone. One-time GM ready pass flips world actors and placed tokens that were still off. **Skipped:** `flags.draw-steel-ghostwire.kind` `node` / `node-template` / `kiosk` / `vehicle` / `drone` (Wire nodes, kiosks, machine templates, Nox freighter). Bestiary + pregen + summon creature stubs stamp vision on in source JSON. Overlay / Jacked In tints still need Scene Token Vision. No Gold Line `{ force: true }`, no PDF.
- `0.3.66` — **Wired Console ready hook: do not pass Array.filter's index as a Foundry flag scope.** `isTemporaryConsoleVerb(item, moduleId)` used as `.filter(isTemporaryConsoleVerb)` received index `0` as `moduleId`, so `item.getFlag(0, "temporaryConsoleVerb")` threw `Flag scope "0" is not valid or not currently active` during `Game.setupGame`. Wrap callbacks (`item => isTemporaryConsoleVerb(item)` / `isOffSheetMatrixVerb`); ignore a second arg that is not a non-empty string and use `MODULE_ID`. Same harden on `hasHideInSheetFlag` / hide-in-sheet stamp. **Food/chem kiosk rows get module `assets/icons/consumables/*.svg`** (stock Foundry 14 has no `icons/consumables/…` game-icons tree); kiosk list falls back food vs chem vs `icons/svg/item-bag.svg`. **Street-snack ¥:** Brick Bar ¥3, Buzz-Can ¥4, Lyte-Pouch ¥5, Shift Chews ¥6, Stall Ramen ¥10, Grease Box ¥12 (was 20–80; Michael: “30 for a brick bar is crazy”). Chems stay Kickwire ¥400 / Clearline ¥350 / Numb-Tap ¥250 / Red Dust ¥600. No Gold Line `{ force: true }`, no PDF.
- `0.3.65` — **Scene kiosk type presets + street consumable SKUs (B119) + default kiosk art.** Token controls › cash register opens a type picker (Food / Medical / Tools / Armor / Weapons / Drones / empty). Placing from a type names the Actor (e.g. Street Food Kiosk) and pre-fills `flags.draw-steel-ghostwire.inventory` from catalog UUIDs (price null = catalog ¥). Restock from preset on the Director shop. New Gear pack shelf **Street Consumables**: six food/drink/supplement SKUs and four chems with real doses (Kickwire, Clearline, Numb-Tap, Red Dust) — maneuver use-ability, Active Effects, temp Stamina; Red Dust also +1 Taint. Default token is Michael’s circular street-kiosk plate (`assets/tokens/kiosks/kiosk-merchant.{png,webp}`). Infinite stock still. No Gold Line `{ force: true }`, no PDF. Spike: `docs/spikes/B119-KIOSK-PRESETS-CONSUMABLES.md`.
- `0.3.64` — **Wired Console list sort + hover names; Ping (and the other eight Matrix Verbs) stay off hero/NPC sheets.** Nodes and Connections sort **revealed first, then A–Z by name** (Director sees hidden nodes under the revealed group; players already only see revealed). Hover a truncated node or Connections name for the full string (`title` + Foundry `data-tooltip`). B117 lock: 0.3.60–0.3.61 temp embeds for `AbilityModel#use` were leaking onto the Draw Steel sheet because hide looked for `data-item-id` while DS 1.1.2 rows use `data-document-uuid` / `_prepareAbilitiesContext`. Fix: stamp `flags.draw-steel.hideInSheet` on temps, filter abilities context, CSS + render hooks (hero/NPC/retainer + ActorSheetV2), ready deletes only orphan temps that are **not** backing a chat `abilityUuid`. Chat tier text still resolves. No Gold Line `{ force: true }`, no PDF, no Ping doctrine rewrite.
- `0.3.63` — **Wired Console Connections: node Actors always show Connected.** Placed Wire nodes (`kind: "node"` — Hotel Interface, Light Control, maglocks, Hosts) are infrastructure, not runners. The roster chip reads **Connected**, never Disconnected. They are **not** the Matrix Verb actor: click a node row to select/pan that board node; the verb strip keeps the last runner (combatant / first on-net hero or NPC). `useConsoleVerb` refuses a node actor. Ping vs Read/Write / ICE docs from **0.3.62** ship in this PR. No Gold Line `{ force: true }`, no PDF.
- `0.3.62` — **Ping vs Read/Write (and ICE) doctrine in the Ping card and Wire rules.** Ping is the lightest “touch this system” Matrix Verb — a nudge, not a deep hack (flick lights; tap a maglock to see if it’s live; test whether a cam answers / one-frame glitch). It does **not** unlock a maglock for entry or kill a camera feed — those are **Read/Write** (change data/settings/state). **Track 1 only (no ICE);** ICE is Track 2. Ping does not bypass or defeat ICE; an ICE-guarded / Track 2 host is Programs / payload Runs / real breach. Director may flavor a failed poke but Ping never opens the guarded system. Lang Story/Effect + RAW `21` + Hacker doctrine + Foundry notes. VOIDMARK index rebuilt. Not Console Wire ping/spoof (B106). Docs/lang only. No Gold Line `{ force: true }`, no PDF.
- `0.3.61` — **Matrix Verb chat: keep the temp embed so Draw Steel can print tier text.** Overlay Search (and the other eight applet verbs) still roll, but 0.3.60 deleted the temporary Item in `useConsoleVerb`'s `finally` before Draw Steel 1.1.2 chat `fromUuidSync(abilityUuid)` — AbilityUsePart / AbilityResultPart then showed **Failed to Find Item for this ability roll** and no Search (etc.) `power.effects` flavor. Fix: keep the flagged embed after a successful card, reuse it on the next fire of the same verb, drop it only if this call created it and the dialog cancelled. Sheet hide + ready leftover strip stay (B117). Soft Trace still reads `abilityResult.tier` off the card. No Gold Line `{ force: true }`, no PDF.
- `0.3.60` — **Matrix Verb fire: real Power Roll + chat from Overlay / Jacked In.** Overlay Search (and the other rolling applet verbs) no longer open an empty configure dialog that produces no dice and no card. Root cause: `resolveVerbItem` built an ephemeral Item (`new Item(data, { parent })`) that was not in the actor collection. Draw Steel 1.1.2 `AbilityModel#use` requires an embedded ability (`abilityUse.abilityUuid` is a DocumentUUIDField). Fix: temporary embed flagged `temporaryConsoleVerb`, `AbilityModel#use`, then delete — verbs stay off the sheet (B117). Hacking / Jacked In edges and Overlay meat bane still come from the Wired use patch. Soft Trace flags land on `messageOptions.data`. No Gold Line `{ force: true }`, no PDF.
- `0.3.59` — **Scene kiosk merchant (B118).** Placeable NPC Actor stub (`kind: kiosk`) sells curated Item UUIDs for nuyen (`system.hero.wealth`). Director names the merchant/corp, stocks Ghostwire Gear (optional ¥ override), sets range in grid squares (default 2). Players open only in range; GM always. Purchase deducts ¥, copies the Item onto the buyer, chat logs. Infinite stock. Token controls › cash register or drag from Summons › Kiosks. Does not touch Gold Line. Spike: `docs/spikes/B118-SCENE-KIOSK-MERCHANT.md`.
- `0.3.58` — **Director Taint +1.** GM-only: Token controls biohazard, hero token HUD, or drag **Director: Taint +1** from **Ghostwire Macros**. Targets targeted tokens, else the controlled/selected token. Heroes (or any actor that already has the Taint flag). `incrementTaint(actor, 1)` clamps 0–12 on `flags.draw-steel-ghostwire.taint`, chat/notify with the new score + band, warns at 12. Per-scene cap stays a table call. No Gold Line, no PDF. Spike: `docs/spikes/B80-CORRUPTION-TAINT-TRACK.md`.
- `0.3.57` — **Linked documented end-to-end (RAW journals + VOIDMARK).** Four states already shipped in **0.3.56**. This bump completes SoR so rules/AI tools no longer say “only Overlay and Jacked In.” RAW `03`/`04`/`19` + Hacker doctrine + manuscript glossary + Foundry notes + journals regenerated. VOIDMARK index rebuilt (`data/voidmark-rules-index.json`); connection-state / Overlay / Jacked In / Connect queries return **Linked**. Lang ping whisper empty-state names on-net (Linked included). No Gold Line `{ force: true }`, no new art, no PDF redo.
- `0.3.56` — **Linked Wire connection state + Console pan-to-node.** Four states: Disconnected / **Linked** / Overlay / Jacked In. Connect (from Disconnected + interface) lands in Linked, not Overlay. Linked is on-net for comms / ID / packets (post-radio Wire default); meat rolls normal; no Jacked In Wired edge; Broadcast and Wire-ping receive work; Scan / Navigate / Ping / Search / Read-Write / Programs / payload Runs still need Overlay or Jacked In. Toggle steps **Linked → Overlay → Jacked In → Linked**. Jack Out from any on-net state → Disconnected. Status `ghostwire-linked`. Scan / Search / Watchdog can find any non-Disconnected presence (Linked = soft presence). Director Console (and minimap) list click **pans/centers** the canvas on that node’s token and controls it when possible. No Gold Line `{ force: true }`, no PDF, no AI art. RAW: `docs/raw/21-the-wire.md`.
- `0.3.55` — **Deadhead hangout Scene removed permanently.** Deletes the Beat 0 hangout inject kit (plate, scene template, register, lang keys, hangout smoke). Beat 0 stays table procedure (Mama wafer, intel chase, call Nox) without a hangout Scene. Canyon remains **SKIPPED** (separate). Gold Line, Mama’s Club, character Kits, Mama Brief, ARG capsule, aerial recon, freighter, ARG bestiary untouched. Worlds that already received the inject must delete that Scene **manually** — the module does not force-delete world Scenes. Spike: `docs/spikes/B107-DEADHEAD-HANGOUT.md` (**KILLED / REMOVED**).
- `0.3.54` — **Ten Conglomerates art + Deadhead aerial recon + Nox freighter Actor.** Megacorp Host skins (`assets/tokens/wired/node-host-{ticker}.{png,webp}`) in the Wired node library; generic **Host** stays default. Brand marks at `assets/brands/megacorps/brand-{ticker}.{png,webp}` with one lore journal per conglomerate (Ghostwire Lore → Ten Conglomerates). Nox’s trash freighter: Vehicles Item **and** placeable Actor in **Ghostwire Runs — Deadhead Actors** (`src/packs/deadhead/nox-trash-freighter.json`, suggested token 4×6). **Gold Line aerial recon** handout in **Ghostwire Runs → Deadhead**. Does not touch the live Gold Line Scene. No PDF redo. Tickers: `docs/rulebook/MEGACORP-TICKERS.md`.
- `0.3.53` — **Matrix Verbs from the node the player faces (B117).** All nine (Connect, Jack Out, Toggle, Scan, Navigate, Ping, Broadcast, Search, Read/Write) fire from the Wired node token/panel (same DS power roll as sheet abilities). Connect needs a tagged Wire interface (street Commlink / Burner / Pocket Sec / deck / datajack / trodes) or Technomancer Resonance. Director Console still has board + roster + a shared verb strip — players do not need the GM to click Console. Soft Trace +1 on tier-1 active rolled verbs; Scan stays observation. Stripped from hero `defaultItems`, pregens, NPC Wire Kit stamp, and Mama Cassavir. Programs stay on the sheet — anyone baseline vs Hacker Programs vs Technomancer Resonance is unchanged. No Gold Line rewrite, no PDF, no AI art. Spike: `docs/spikes/B117-CONSOLE-MATRIX-VERBS.md`.
- `0.3.52` — **Deadhead Gear SKUs + Mama names + NPC portrait notes.** Mama’s Deadhead Brief and ARG Courier Capsule ship as Ghostwire Gear plot items (`src/packs/gear/general/plot/`) with Deadhead gallery art at `assets/items/deadhead/` (png + webp; not regenerated). Director journal Items page `@UUID`s the SKUs. Mama Cassavir embedded item `name`s are English (Seize Control, Lockout, Puppet Strings, Chokepoint, Matrix Verbs, chrome). Locked portrait notes: `docs/rulebook/ART-NPC-PORTRAIT-NOTES.md`. Does not touch Gold Line. Spike: `docs/spikes/B104-DEADHEAD-FOUNDRY-RUN-PACK.md`.
- `0.3.51` — **Wire Atlas token art (B116).** Michael Relay / Host / Segment tokens at `assets/tokens/wired/node-relay|host|segment.{png,webp}`. Catalog `placeholder: false`; family / altitude unchanged. Place on canvas stamps the WebP. Topology brief stays **0.3.50**. Does not touch Gold Line. No PDF redo. Spike: `docs/spikes/B116-WIRE-ATLAS.md`.
- `0.3.50` — **Wire Atlas topology (B116).** RAW + Foundry journal: three altitudes (district Relay/Host → facility Segment → room Device); Scan Reach = hops on the current scene graph; dig-down procedure; catalog stubs at `assets/tokens/wired/node-relay|host|segment` (no generated art). Mama is **0.3.48**; Wired Gold Line playtest shipped **0.3.49** (PR **#34**). Does not touch Gold Line. Spike: `docs/spikes/B116-WIRE-ATLAS.md`.
- `0.3.49` — **Wired Gold Line playtest (B112 / B113 / B114 / B115 / B116).** GM **Auto-nodes from Scene** builds `{Room} - Light Control` (T1 R1), `{Room} - Maglock Door N` (T1 R2), and `{Room} - Cam Controls N` (T1 R1, cam lights / named cams) from named lights + wall doors, places hidden 0.25 tokens on the current Level with **B113** library art (`assets/tokens/wired/node-*.webp`; generic Track 1/2 templates stay when `tokenStyle` is empty), skip/replace idempotent. Wired Node Map gets force/cluster layout, label collision avoidance, zoom/pan, truncated pills. Droppable **Wire Kit — Matrix Verbs** stamps the nine hero verbs onto an NPC (Console / HUD one-click). No bestiary default. **B116** eight-style node token library (light-control, maglock, black-ice, normal-ice, mechanical, turret-controls, cam-controls, data-vault); Director **Token art** select writes `tokenStyle`. Does not touch the Gold Line live-scene lock. Spikes: `docs/spikes/B112-SCENE-WIRE-AUTO-NODES.md`, `B113-LIGHT-MAGLOCK-TOKEN-ART.md`, `B114-NODE-MAP-READABILITY.md`, `B115-NPC-WIRE-KIT.md`, `B116-NODE-TOKEN-LIBRARY.md`.
- `0.3.48` — **Mama Cassavir identity lock:** Cyborg hacker Controller (not human Rival Tactician artillery). Soft chrome sells the grandmother look; Wire kit + people; holdout is implant-weapon backup. Negotiation-first named boss, echelon 2. Reach Handbook Switchboard + L3 lore synced (player grandmother booth; Director cyborg truth). Michael’s split-face portrait ships as `assets/tokens/bestiary/mama-cassavir.{png,webp}`; Actor `img` + `prototypeToken.texture.src` point at the WebP. Rebuild `node tools/build-packs.mjs bestiary reach-handbook lore`.
- `0.3.47` — **ARG Argent Exchange bestiary portraits (B111).** Michael’s ARG Corporate Enforcer, ARG Response Lieutenant (Goliar), and ARG Security Officer tokens ship under `assets/tokens/bestiary/arg/` (PNG originals + 1024² WebP). Actor `img` + `prototypeToken.texture.src` point at `modules/draw-steel-ghostwire/assets/tokens/bestiary/arg/…`. Display names and Deadhead opposition lines use the ARG-prefixed names; `flags.draw-steel-ghostwire.faction = "arg"`. Does not touch the Gold Line live-scene lock. Spike: `docs/spikes/B111-ARG-TOKEN-ART.md`.
- `0.3.46` — **Wired node Place scale (B110):** Place Node tokens are 0.25 grid. Spike: `docs/spikes/B110-WIRED-NODE-TOKEN-SCALE.md`.
- `0.3.45` — **Deadhead playtest: Wire ping + Technomancer Whiteout.** Wired Console GM **Wire ping/spoof** (short text + Send; log for anyone with Console open; public chat or whisper to Overlay / Jacked In; last ~20 on `wiredPings`). Technomancers compile payload magazines onto **Wired Native** (Resonance / body, no deck); **Sabbat Vane** starts with Whiteout ×2 compiled like Kessic. Not an auto-grant. Spikes: `docs/spikes/B106-WIRED-CONSOLE-WIRE-PING.md`, `docs/spikes/B109-TECHNOMANCER-DECKLESS-PAYLOADS.md`. Does not touch the Gold Line live-scene lock.
- `0.3.44` — **Wired node Place uses current Level (B108):** Place on canvas stamps Foundry V14 `level` + elevation so Interior tokens stay on Interior, not Roof. Spike: `docs/spikes/B108-WIRED-NODE-LEVELS-ELEVATION.md`.
- `0.3.43` — **Deadhead Beat 0 hangout (B107):** shipped a still hangout Scene inject (later **REMOVED permanently 0.3.55**). Canyon plate is **SKIPPED** — narrate the approach. Spike: `docs/spikes/B107-DEADHEAD-HANGOUT.md`.
- `0.3.42` — **Deadhead Director journal:** Ghostwire Runs → Deadhead now ships the full run journal (Overview, Trace 1–12 + call-home, cargo consist L1–R3, Beats 0–5, Items, Opposition, Foundry checklist). Cargo maglev — not a passenger train. Gold Line world Scene stays Michael’s (no inject / walls / lights / tiles edits). Generate with `node tools/deadhead-to-journals.mjs`; rebuild `node tools/build-packs.mjs runs`. Spike: `docs/spikes/B104-DEADHEAD-FOUNDRY-RUN-PACK.md`.
- `0.3.41` — **Gold Line inject without touching live worlds (B106):** new worlds get Level interior MP4 + one roof tile at 0,0 elev 1. Existing `goldLineScene` worlds are **never rewritten** on ready. `{ force: true }` is GM-opt-in only. Prefer `*-loop.mp4`. Skip seek when duration is non-finite. Spike: `docs/spikes/B106-GOLD-LINE-MAP-PACK.md`.
- `0.3.40` — **Deadhead Gold Line cargo remap:** fold cargo maglev fiction into the live SoR + Ghostwire Runs map notes (Michael: RUN WITH, 2026-09-20). Not a passenger train. Dual Hammerhead L1–R3; freight Enforcers L1–L2; 4 Security+Lt L3; Wire R2/R1/R3. Sidecar `docs/directors/runs/deadhead/GOLD-LINE-CARGO-REMAP.md` LOCKED / folded.
- `0.3.38` — **Corruption / Taint track (B80):** RAW `docs/raw/27-corruption-taint.md` — Taint 0–12 bands Clean / Marked / Stained / Claimed / Hollowed; +1 max per scene except pact; rest never cleanses; chrome does not raise Taint; Mutant Corruption Load stays retired. Hero sheet Stats field writes `flags.draw-steel-ghostwire.taint`. Director notes: `docs/directors/corruption-taint.md`. Spike: `docs/spikes/B80-CORRUPTION-TAINT-TRACK.md`.
- `0.3.37` — **Gold Line loop hotfix (B106):** stop HEAD-probing webm (that fell back to stills). Level background now sets video loop/autoplay; `ensureGoldLineScene({ force: true })` restamps the roof tile onto the loop. Spike: `docs/spikes/B106-GOLD-LINE-MAP-PACK.md`.
- `0.3.36` — **Deadhead Gold Line map pack (B106):** CyberMaps Hammerhead dual stitch (no generated train). Stills + VP9 loops under `assets/maps/battlemaps/gold-line/`. World inject creates **Scenes → Deadhead → Gold Line** (interior loop background, roofs Surface overhead, grid 208 / 5 ft). Director SoR + **Ghostwire Runs → Deadhead** map notes carry the L1–R3 beat remap. Spike: `docs/spikes/B106-GOLD-LINE-MAP-PACK.md`.
- `0.3.35` — **Bestiary + L≤4 summon portraits (B103):** 40 bestiary (humanoids + ICE) + 17 summon WebPs under `assets/tokens/{bestiary,summons}/`. Actor `img` and `prototypeToken.texture.src` point at `modules/draw-steel-ghostwire/assets/tokens/…/<slug>.webp`. Apply: `node tools/apply-bestiary-portrait-art.mjs`. Spike: `docs/spikes/B103-BESTIARY-HUMANOID-PORTRAITS.md`.
- `0.3.34` — **Whiteout payload (B105):** Restricted Echelon-1 Trace-scrub magazine (¥3,500 / chip; Craft Hacking steep/hard). Foundry Matrix pack SKU + Run Whiteout (B51). Kessic Draye starts with a 2-fire magazine compiled on Switchblade / Street Deck. Spike: `docs/spikes/B105-WHITEOUT-PAYLOAD.md`.
- `0.3.33` — **Deadhead Director SoR + Runs pack scaffold (B104):** playtest journal SoR at `docs/directors/runs/deadhead/DEADHEAD-GOLD-LINE.md`. New GM-only JournalEntry pack **Ghostwire Runs** (`packs/runs`) with empty folder **Deadhead**. Journals / items / scenes / art still to ship. Spike: `docs/spikes/B104-DEADHEAD-FOUNDRY-RUN-PACK.md`.
- `0.3.32` — **Armor + weapon item art (B102):** 22 armor/shield + 49 weapon portraits under `assets/tokens/{armor,weapons}/`. Gear pack Item `img` fields and matching pregen embedded treasure Items point at `modules/draw-steel-ghostwire/assets/tokens/…/<dsid>.webp`. Apply: `node tools/apply-gear-token-art.mjs`. Spike: `docs/spikes/B102-ARMOR-WEAPON-ITEM-ART.md`.
- `0.3.31` — **Vehicle + drone tokens (B101):** 36 drone + 32 vehicle chassis portraits (1024² WebP) under `assets/tokens/{drones,vehicles}/`. Ghostwire Vehicles & Drones Item `img` fields point at `modules/draw-steel-ghostwire/assets/tokens/…/<dsid>.webp`. Apply: `node tools/apply-machine-token-art.mjs`. Spike: `docs/spikes/B101-VEHICLE-DRONE-TOKEN-ART.md`.
- `0.3.29` — **VOIDMARK knowledge index:** district / hive lore (Switchboard, the Flats, Ossian Reach) retrieves from shipped Reach Handbook + L3 (and other L-chips / Wired Flats gazetteer). Rebuild: `node tools/build-voidmark-index.mjs`. Notes: `docs/spikes/B82-VOIDMARK-AI-APPLET.md`.
- `0.3.28` — **VOIDMARK default model:** `voidmarkModel` / `DEFAULT_MODEL` is now `grok-4.6` (`grok-3` 404s on current xAI). Pick any id your key lists (`grok-4.6`, `grok-4.5`, etc.). Notes: `docs/spikes/B82-VOIDMARK-AI-APPLET.md`.
- `0.3.27` — **VOIDMARK chat (B82):** ApplicationV2 in-module AI applet (Token controls › ghost, or a keybinding). Module Configuration holds an OpenAI-compatible base URL (default xAI `https://api.x.ai/v1`), secret API key, model (default `grok-4.6` as of 0.3.28), temperature, player vs GM-only access, and editable VOIDMARK system instructions. Rules questions retrieve from a shipped lexical index of Ghostwire RAW (`data/voidmark-rules-index.json`; rebuild with `node tools/build-voidmark-index.mjs`). Not a live Cursor/Grok Bot bridge. Notes: `docs/spikes/B82-VOIDMARK-AI-APPLET.md`. *Pending Foundry verification.*
- `0.1.0` — skeleton
- `0.1.1` — lang pass (characteristics + nuyen + sheet labels) and CSS style tokens
- `0.1.2` — Foundry spike: **Ghostwire Origins** compendium with Pure Human (ancestry) and its signature trait Detect the Supernatural (trait + maneuver), cloned from DS Human. Purchased traits not yet implemented.
- `0.1.3` — Pure Human purchased traits: 3-point picker with Can’t Take Hold (1), Perseverance (1), Resist the Unnatural (1), Determination (2), and Staying Power (2), cloned from DS Human. The build script now writes embedded Active Effects.
- `0.1.4` — Lang: item type labels Culture → **Background**, Career → **Profession** (`TYPES.Item.*`), so the hero sheet header shows "+ Add Background" and "No Profession". Class is unchanged.
- `0.1.5` — Corran ancestry: signature trait Labor Brand (a reskin of Runic Carving; the Light brand is an effect you toggle on the sheet) and a 3-point picker with Great Fortitude (2), Grounded (1), Hardened Hide (2), Stand Tough (1), and Stone Shaper (1), cloned from DS Dwarf.
- `0.1.6` — Ghostwire Origins now has one folder per People. Adds the Elvani ancestry: signature trait Corp Glamor and a 3-point picker with Glamor of Terror (2), Graceful Retreat (1), High Senses (1), Otherworldly Grace (2), Revisit Memory (1), and Unstoppable Mind (2), cloned from DS High Elf.
- `0.1.7` — New heroes get **Drive** (vehicles) instead of the Draw Steel **Ride** move action. Drive lives in a new **Ghostwire Abilities** compendium; `scripts/module.mjs` swaps it into `ds.CONFIG.hero.defaultItems` at init.
- `0.1.8` — Goliar ancestry (in its own Origins folder): signature trait Relentless and a 3-point picker with Bloodfire Rush (1; its +2 speed is an effect you apply by clicking a link in the trait), Glowing Recovery (2), Grounded (1), Nonstop (2), and Passionate Artisan (1), cloned from DS Orc.
- `0.1.9` — Changer ancestry (in its own Origins folder), cloned from DS Dragon Knight: signature trait Beast-Hide and a 3-point picker with Pack Guard (1), Feral Cry (2), Savage Burst (2), Layered Hide (1), Beast’s Resolve (1), and Beast Movement (2). A required **Lineage** pick gives Raven Walk + Fly, Rat size 1S, or Wolf +2 speed. **Changer Forms** has three form effects (Human, the default; Hybrid; Beast), and `scripts/module.mjs` keeps only one enabled and warns if a Changer is created without a lineage.
- `0.1.10` — Revenant ancestry (in its own Origins folder), cloned from DS Revenant. Signature trait Tough But Withered (immunity equal to level to cold, corruption, lightning, and poison; fire weakness 5; being inert, and fire destroying an inert body, are rules text only). A required **Former Life** pick grants one of `Former Life: Pure Human / Corran / Elvani / Goliar / Changer / Mutant / Cyborg`, which stays on the sheet. The purchased traits are Bloodless (2), Previous Life: 1 Point (1), Previous Life: 2 Points (2), Undead Influence (1), and Vengeance Mark (2, grants the Vengeance Mark maneuver and the Detonate Sigil signature ability). Previous Life opens a pick of one real Origins trait of that cost; `scripts/module.mjs` disables any trait that isn’t in the Former Life People’s Origins folder. Size and speed need no effects (every Ghostwire People is 1M and hero speed is 5), so a 1S former life (a Rat-lineage Changer) uses **Revenant (Small)** (size 1S, 3 points). Mutant and Cyborg have no Origins traits yet, and each Previous Life trait can be bought once. No Decay ladder.
- `0.1.11` — Mutant ancestry (in its own Origins folder), cloned from DS Devil: signature trait Aberrant Rapport (one interpersonal skill, plus an edge to discover motivations and pitfalls) and a 3-point picker with Barbed Mutation (1), Warped Legs (1, speed 6), Taint Flare (1, grants a triggered ability), Taint Sight (1), Ossified Crown (2, saves on 5+), Prehensile Mutation (2, can’t be flanked), and Membrane Wings (2; toggle the echelon 1 or echelon 2+ fly effect while flying). The optional Corruptive Flaw is description only, with no Corruption Load meter. Revenant Former Life: Mutant now offers these traits to Previous Life.
- `0.1.12` — Cyborg ancestry (in its own Origins folder), cloned from DS Time Raider. Signature trait Cortical Firewall (psychic immunity equal to level) plus a free **Arcane Severance** feature holding the hard constraints: no Magic abilities, no Elementalist or Street Priest, tech-only recovery, and a System Crisis pointer. There’s a 3-point picker with Penetration Optics (1, grants a maneuver), Predictive Sensors (1), Auxiliary Limbs — Athletics (1), Auxiliary Limbs — Combat (2), Installed Suite (2: pick Kinetic Driver, Particle Lance, or Servo Overclock; these use a **Tech** keyword that `scripts/module.mjs` registers, not Psionic), and Locked Processors (2, can’t be dazed). Particle Lance follows the Ghostwire text, so it has no slide. Cyborgs don’t use the Chrome chapter. Revenant Former Life: Cyborg now offers these traits to Previous Life.
- `0.1.13` — Street-themed default hero actions with unchanged mechanics: Charge → **Rush** (the Charge keyword label also reads Rush), Defend → **Take Cover**, Heal → **Patch Up**, Aid Attack → **Spot Target**, alongside Ride → Drive. They are Ghostwire Abilities copies that `scripts/module.mjs` swaps into the hero default items. Hide, Search for Hidden Creatures, Use Consumable, and Make/Assist Test are rules text in Draw Steel, not hero ability items, so they aren’t renamed.
- `0.1.14` — The Origins compendium is now labeled **Ghostwire Ancestries** in Foundry. Its pack id and path stay `origins`, so existing `Compendium.draw-steel-ghostwire.origins.*` UUIDs and links don’t change.
- `0.1.15` — **Body Integrity** (actor flag, shown at the top of the hero Stats tab; living heroes 20/20; Cyborgs see N/A), new heroes start with **¥5,000**, and a **Ghostwire Chrome** compendium with five Standard implants that have working effects: Datajack (1), Cyber-Eyes (2, edge on Alertness and Search), Reaction Enhancer (3, +1 Disengage), Dermal Plating (4, damage immunity 2), and Cyberlimb (Arm) (5, +1 melee weapon damage). Dropping an implant on a hero spends its Integrity; deleting it refunds 75%. Cyborgs and heroes without enough Integrity are blocked.
- `0.1.16` — Ghostwire Chrome now has 13 Standard implants, each with a price and Availability (the header line in its description, plus a line under the name on the item sheet). New implants: Vocal Modulator, Implant Weapon (grants Spur Strike), Internal Air / Filtration, Cyber-Ears, Running Gear, Skillwires / Encephalon, Muscle / Bone Lacing, and Wired Reflexes. Location slot caps are enforced (Nervous System 1, so Reaction Enhancer and Wired Reflexes can’t stack).
- `0.1.17` — **Ghostwire Kits** compendium with all 21 Kits in role folders, cloned from the Draw Steel kits with stock bonus numbers as a provisional benchmark and reskinned signature abilities. The Duelist signature uses the Tech keyword instead of Psionic. Kit sheets relabel weapon categories: Firearm / Bow, Unarmed / Cyberlimb, Whip / Monowhip.
- `0.1.18` — **Ghostwire Gear** compendium: 17 Kit-qualifying weapons, shields, and special gear plus 4 armors, each with ¥, Availability, and the Kit categories it satisfies (in the description header and under the name on the item sheet). Street gear adds no numbers because the Kit supplies doctrine. Armor Stamina is a no-Kit-only toggle, and a few premium items have small wielded toggles.
- `0.1.19` — **Ghostwire skills**: the 44 skills in 6 groups (Action, Technical, Knowledge, Social, Vehicle & Drone, Magic & Supernatural) replace the Draw Steel skill list everywhere skills are picked or shown. Draw Steel skill and group grants (stock Backgrounds, Professions, classes) translate to Ghostwire skills. Module content that referenced Draw Steel skills now uses Ghostwire skills.
- `0.1.20` — **Ghostwire Classes** compendium with the **Operator** (Adrenaline), cloned from the Draw Steel Fury: class, three Origins (Corp-Milspec, Merc with two Kits, Street-vet), Ghostwire signatures Controlled Pair + Suppressing Fire, the Ghostwire 1–5 Adrenaline base band, and every level 1–10 feature and 7/9/11 ability with Draw Steel mechanics under chapter names.
- `0.1.21` — **Ghostwire Backgrounds** (8) and **Ghostwire Professions** (15). A Background grants one fixed skill and a choice of 1 from four; a Profession grants one fixed skill and a choice of 2 from four. Skill pickers leave out skills the hero already has. The hero sheet’s **+ Add Ancestry / Background / Profession** buttons open the Ghostwire compendiums.
- `0.1.22` — Operator polish: feature levels follow `01-operator.md`; heroic abilities use the Tech keyword instead of Magic; signatures and base band roll with Reflex; skill picks use Ghostwire groups; Merc gets a Field Arsenal feature (two Kits, preferred Kit); Street-vet picks a Loadout Damage Type; remaining Fury flavor text rewritten. Heroic abilities can’t be used in combat without enough heroic resource (Adrenaline).
- `0.1.23` — **Full Master Gear List import** (`docs/masters/GHOSTWIRE_GEAR_MASTER.md`): every table row is an item — **Ghostwire Gear** (107: general & lifestyle, armor, weapons), **Ghostwire Mods** (16), **Ghostwire Matrix** (33), **Ghostwire Vehicles & Drones** (31), **Ghostwire Foci** (41). Grade is Draw Steel **echelon 1–4 + Availability band** (no Item Tier). Replaces the 0.1.18 Kit-subset gear.
- `0.1.24` — **Mods expansion:** Ghostwire Mods now has Weapon, Vehicle & Drone, **Armor & Shield** (7), and **Gadget** (8) mods (31 total); Ghostwire Matrix adds **RCC Autosofts** (4). Every item with mod slots lists the mods that fit it; items with no mod family (foci, Field Surgery Kit, Designer Threads, Faraday Bag) have 0 slots. §Craft text is the downtime Project procedure keyed to Repair, Electronics, Hacking (autosofts: Hacking or Rigging), or Cybertech — no “Gunsmithing”. Rules: `docs/rulebook/14-mods.md` (mod doctrine + Invent a Mod).

- `0.1.25` — **Scout** class (Advantage) in **Ghostwire Classes**, cloned from the Draw Steel Shadow: class, three subclasses (**Hunter** — Ironclad Longshot, **Ghost** — Ferrum Deep-Dark, **Face-in-crowd** — Nyx Mask-Line), four signatures, the 3/5/7/9/11 Advantage bands, and every level 1–10 feature, per `docs/rulebook/02-scout.md`. New **Chrome** and **Optics** ability keywords. *Pending Foundry verification.*
- `0.1.26` — **Ghostwire Perks** compendium: all 47 Draw Steel perks reskinned for the Reach, in one folder per perk type (crafting, exploration, interpersonal, intrigue, lore, supernatural), plus the three perk abilities (Veil Trick, Ghost Touch, Wire Whisper) cloned into the pack. Class perk grants now list the matching Ghostwire perks as choices; Draw Steel perks are refused and removed from the perk registry. Rules: `docs/rulebook/17-perks.md`. *Pending Foundry verification.*
- `0.1.27` — **Matrix Verbs + Wired connection states (B23a):** every hero gets the nine Matrix Verbs (Ghostwire Abilities › Matrix Verbs, **Wired** keyword, no cost). Connect enters **Overlay**, Toggle Connection State switches Overlay ↔ **Jacked In**, Jack Out disconnects; the state is a token status icon, a sheet status, a Wired box on the Stats tab, and `flags.draw-steel-ghostwire.wired`. Overlay banes real-world ability rolls; Jacked In gives an edge on Wired rolls and blocks real-world rolls; the Hacking skill gives an edge on Wired rolls. Existing heroes get missing verbs once on GM load. Rules: `docs/rulebook/08-hacker.md`; Foundry notes: `docs/rulebook/18-wired-foundry.md`. *Pending Foundry verification.*
- `0.1.28` — **Wired Console (B23b):** an ApplicationV2 popout (Token controls › network button, or an assignable keybinding) for the viewed scene: a live connection roster from the Overlay / Jacked In statuses, and the scene’s nodes stored in `flags.draw-steel-ghostwire.wiredBoard` — Track, Node Rating with the System Stat Card (Breach DC, ICE, Biofeedback, Integrity 12/18/26/36/50), Integrity damage/restore, and a 12-step Trace Alert with a counter-trace warning and reset to 6. The Director edits and reveals nodes; players see revealed nodes and their own connection read-only. Notes: `docs/rulebook/18-wired-foundry.md`. *Pending Foundry verification.*
- `0.1.29` — **Commander** class (Influence) in **Ghostwire Classes**, on the Draw Steel Tactician spine: class, three Command Doctrines (**Street-Fixer**, **Corp-Exec**, **Bard**) with their level 1–9 features, triggered actions, and 2nd/6th/9th-level ability choices; four signatures (**Direct Ally**, **Mark** with a Marked effect, **Read the Room**, **The Right Word**); every 1/3/5/7/9/11-Influence ability from the chapter; and the level 1–10 class features. New **Command** ability keyword. Rules: `docs/rulebook/03-commander-face.md`. *Pending Foundry verification.*
- `0.1.30` — **Languages:** every Draw Steel language keeps its key but shows its locked Ghostwire name (Trade Cant, Corp Cant, Wire Speak, …) on the hero sheet and in language pickers. Map and rules baseline: `docs/masters/GHOSTWIRE_LANGUAGES.md`, `docs/rulebook/19-languages.md`.
- `0.1.31` — **Medic** class (Reagents) in **Ghostwire Classes**, on the Draw Steel Troubadour spine: class, three specializations (**Street-Doc**, **Corp-Medtech**, **Ripperdoc**, including the Nano-Adrenal Auto-Injector capstone) with their level 1–9 features, triggered actions, and 2nd/6th/9th-level picks; three signatures (**First Aid**, **Administer Dose**, **Diagnose**); every 1/3/5/7/9/11-Reagent ability from the chapter plus **Field Synthesis**; and the level 1–10 class features. **Reagents persist across encounters:** no per-turn gain, and combat start doesn't reset them. Rules: `docs/rulebook/04-medic.md`.
- `0.1.32` — **Wrench** class (Uptime) in **Ghostwire Classes**, on the Draw Steel Talent spine with every psionic reference scrubbed: class, three street-taught subclasses (**Drone Jockey**, **Vehicle Rig-Pilot**, **Facility Rigger**) with their level 1–9 feature ladders and full Base / 7 / 9 / 11-cost ability bands; four signatures (**Deploy & Command**, **Rigged Fire**, **Field Repair**, **Override Ping**); **Focus Fire**; Fleet Size, Uptime cap, Jump-In buffer, and **Overclock** features. Uptime gains a flat 1 per turn in Foundry; the rest of the fleet loop is feature text. Drone/vehicle inventories are not in this release. Rules: `docs/rulebook/05-wrench.md`.
- `0.1.33` — **Medic gap locks:** Toxins deal 2 + Instinct over 2 rounds with Weakened (enhanced 4 + Instinct, Weakened and Slowed); a second 9th-level pick for each specialization (**Last Syringe**, **Crash Protocol Override**, **Emergency Excision**); **Anesthetize** is now an Instinct power roll; Reagents still spend outside combat, and Foundry blocks an unaffordable Reagent ability out of combat too.
- `0.1.34` — **Wrench polish:** Overclock is now a real 10th-level capstone (once per encounter on Deploy/Command: Uptime costs −2, machines +2 Integrity buffer); **Jump-In Buffer** rises to +5 at 6th level and +6 at 7th; **Uptime Economy Mastery** at 9th. Three new rigger Kits in **Ghostwire Kits › Tech / rigger** — **Fabricator's Bench**, **Rigger's Harness**, **Field Chassis** — each with a signature ability and a +1 rigging bonus, listed first in the Wrench Kit picker as the subclass starter Kits. **Medic:** Nerve Toxin and Nerve Agent deal 6 + Instinct over 3 rounds; the Medic master matches the chapter locks.
- `0.1.35` — **Elementalist** class (Essence) in **Ghostwire Classes**: class, three specializations (**Pyromancer**, **Stormcaller**, **Geomancer**) with passives at 1/2/5/6/8, signature summons (Ember / Zephyr / Boulder Companion), signature foci from Ghostwire Foci, and specialization ability picks at 2/6/9; three signatures (**Hurl Element**, **Elemental Shaping**, **Read the Weave**); the base / 7 / 9 / 11-Essence workings including **Summon Elemental**; and the level 1–10 class features through **Primordial Reservoir**. Essence drips 1 per turn; Channel and Resonance are feature text. Cyborgs can't be Elementalists: the hero sheet refuses the combination either way (Arcane Severance). **+ Add Class** now opens Ghostwire Classes, and the Elementalist Kit grant offers **No Kit (Pure Caster)**. Rules: `docs/rulebook/06-elementalist.md`.
- `0.1.37` — **Street Priest** class (Conviction) in **Ghostwire Classes**: a level-1 **Light / Dark pact** overlay (the pact-named features — Roster/Ledger, Burgeoning Saint/Rising Adept, Consecrated/Damned Weapon, Ordained/Sworn — offer only your pact's version); three ministries (**Shepherd**, **Templar** with the **Judgment** mark, **Exorcist**) with ladders and ability picks; three class signatures plus two chosen from eight; the 1/3/5/7/9/11-Conviction workings including **Invoke the Pact**; and the level 1–10 features through **Avatar of the Pact**, **Manifest Will**, and **Most Faithful**. Conviction drips 2 per turn; the Prayer Gamble is feature text. Cyborgs can't be Street Priests. Rules: `docs/rulebook/07-street-priest.md`.
- `0.1.38` — **Hacker** class (Bandwidth) in **Ghostwire Classes**: three Hacking Doctrines (**Disruptor**, **Controller**, **Support**) with full level 1–9 ladders; Programs **Seize Control**, **Deep Scan**, **Ghost Signal**, **Kill Switch**, **Failsafe Cascade**, **Network Purge**, **Backdoor Override**; core features through **Ghost in the Machine**, **Infinite Loop**, and **Borrowed Access**. Three street cyberdecks as Kits in **Ghostwire Kits › Tech / rigger** — Nyx Cartel "Switchblade", Ferrum "Padlock-6", Meridian "Lookout" — each with its signature. The nine Matrix Verbs stay on every hero. Cyborgs can be Hackers. Rules: `docs/rulebook/08-hacker.md`.
- `0.1.39` — **Technomancer** class (Resonance) in **Ghostwire Classes**, the ninth class: three Sprite/Resonance Disciplines (**Sprite-Weaver**, **Machine-Whisperer**, **Resonance-Warrior**) with ladders at 1/2/3/5/7/8; three signatures (**Compile Sprite**, **Resonance Strike**, **Resonance Mending**, which heals machines, chrome, and Cyborgs); the 1/3/5/7/9/11-Resonance abilities including **Total Resonance**; and core features through **Master of the Current**. Resonance drips 1 per turn; sprites and biofeedback (5+ Resonance spends only, never lethal) are feature text. Optional light Kit, including **No Kit (Pure Caster)**. Cyborgs can't be Technomancers. Rules: `docs/rulebook/20-technomancer.md`.
- `0.1.40` — **Ghostwire Summons & Machines › Sprites:** 12 Technomancer sprite Actors (Data, Attack, Machine, Ward × Minor, Intermediate, Advanced) as small friendly Draw Steel npcs with playtest Stamina baked at each hybrid tier's entry level (the description gives the live formula). Attack sprites carry a **Code Strike**; the others carry their Data Uplink, Mend, or Ward Screen feature. Rules: `docs/rulebook/20-technomancer.md`; inventory: `docs/masters/GHOSTWIRE_SUPPORT_ENTITIES.md` §3.1.
- `0.1.41` — **Ghostwire Summons & Machines › Elementals & Companions:** the Elementalist's **Ember**, **Zephyr**, and **Boulder Companions** as usable Rank 1 Actors with their signature strikes (Zephyr slides, Boulder shoves), plus drag-to-Scene scaffolds for **Bound Elemental** Rank 1, 2, and 3 and the **Greater Elemental**, with Persistent drain reminders and a Twin Elemental Summon note. Stamina is a provisional 15 × Rank until the Veil entity table locks. Rules: `docs/rulebook/06-elementalist.md`; inventory: `docs/masters/GHOSTWIRE_SUPPORT_ENTITIES.md` §3.2.
- `0.1.42` — **Ghostwire Summons & Machines › Pact Spirits:** the Street Priest's Invoke the Pact entities as three Actors — **Guardian Spirit** (Shepherd, Warding Aegis), **Warrior Spirit** (Templar, Pact Blade), **Hunter Spirit** (Exorcist, Binding Chain) — each with the Invoke the Pact rules on its sheet. One Actor serves both pacts: enable **Pact: Light** or **Pact: Dark** on a placed spirit to make its strikes holy or corruption, tint the token, and record the pact (the two are exclusive). Stamina is a provisional 20 until the Veil entity table locks. Rules: `docs/rulebook/07-street-priest.md`; inventory: `docs/masters/GHOSTWIRE_SUPPORT_ENTITIES.md` §3.3.
- `0.1.43` — **Drones & vehicles on the table:** right-click a drone or vehicle on a hero's sheet (or open the Item) and choose **Deploy** — it creates a linked Actor named after the Item (in the **Deployed Machines** folder), stamps its Stamina (Integrity) and speed, and places its token next to the hero; **Recall** removes the token and Actor while the Item stays. Nine scale-band templates live in **Ghostwire Summons & Machines › Drones & Vehicles (Actors)** (drone micro / small / medium; bike, car, heavy ground, air, water, space). Numbers are provisional until the Machines numeric pass; Deploy needs the Director (Create Actors / Tokens). Band map: `docs/masters/GHOSTWIRE_MACHINE_BANDS.md`.
- `0.1.44` — **Mod install tracker:** right-click a mod on a hero's sheet and choose **Install onto…** to fit it to a weapon, armor, gadget, vehicle, drone, or deck/RCC the same hero owns (the host's mod family must match and it needs free slots), or **Uninstall mod** to free the slots. The host's sheet shows **Mod slots used / max** and lists its installed mods; the mod's sheet shows what it's installed on. Recording an install doesn't roll the §Craft Project. Rules: `docs/rulebook/14-mods.md`.
- `0.1.45` — **Wired node templates:** the Wired Console's Nodes panel has an **Add template…** button for Directors — ten presets (Track 1 and Track 2 at Node Rating 1–5) with the System Stat Card numbers, a player-facing Description, and Director Notes filled in; Track 1 objects carry no Integrity, ICE, or biofeedback. Director reference: `docs/directors/wired-node-templates.md`.
- `0.1.46` — **Wired nodes on the canvas:** when the party jacks in and you move to a matrix map, set **Wired map for** in the Wired Console header to the original Scene and the Console uses that Scene's nodes. Select a node and **Place on canvas** to drop a token for it (hidden until revealed); Track 2 tokens show an Integrity bar that stays in sync with the Console both ways, and **Remove from canvas**, deleting the node, or resetting the board cleans the tokens up. Notes: `docs/rulebook/18-wired-foundry.md`.
- `0.1.47` — **Wired vision:** a hero's token view now changes with their connection — **Overlay** adds a readable cyan/pink HUD wash, **Jacked In** drops meatspace into dark, desaturated shadow with neon light punching through, and Jack Out restores normal vision. It shows for whoever is looking through that token (its players, or a GM controlling it) on Scenes with Token Vision on. Notes: `docs/rulebook/18-wired-foundry.md`.
- `0.1.48` — **Full drone & vehicle catalog:** Ghostwire Vehicles & Drones now has every chassis from the Drones and Vehicles chapters — **36 drones** (clunkers like Tape-Eye and Junkbug up to Razorwing and Iron Mantis) and **32 crewed platforms** (Clunker, Scrap-Bike, Grey Cab, Flatbed, Harbor Cutter, Cage, River-Fang, Drop-Sled, Spider-Frame, Void-Runner, and the rest). Every Item shows its chapter tags, crewed vehicles record their Speed band, and every one Deploys to the matching band token. Band map: `docs/masters/GHOSTWIRE_MACHINE_BANDS.md`.
- `0.1.49` — **Deck & RCC software:** Matrix programs (Reader, Sneak, Skeleton, …) now install onto decks and autosofts onto RCCs with **Install onto…**, filling mod slots like any mod (programs won't go on an RCC, autosofts won't go on a deck). Any installed mod can be switched off and on with **Deactivate / Activate** — it keeps its slot — and host sheets show each one as on or off. While on, **Reader** gives an edge on Scan, Search, and Deep Scan, **Skeleton** on Seize Control, and **Targeting Autosoft** on the Wrench's drone gunnery; the other programs apply their effect text at the table. Rules: `docs/rulebook/14-mods.md`.
- `0.1.50` — **Ghostwire Bestiary (wave 1):** a new Director-only **Ghostwire Bestiary** Actor compendium with 35 reskinned Draw Steel monsters in six folders — street and corp humans (Corp Security Officer, Gang Boss, Street Punk, …), Rivals for each Ghostwire class, Ironclad Legion troops, Veil undead, Chrome Raiders, Signal Talkers, and jungle beasts. Draw Steel stats and abilities are unchanged; names and descriptions are Ghostwire. The review of all 483 Draw Steel monsters (187 Keep, 224 Adapt, 56 Skip, 16 deferred hazards) is in `docs/masters/GHOSTWIRE_BESTIARY_DS_INVENTORY.md`. *Pending Foundry verification.*
- `0.1.51` — **Bestiary firearms & chrome pass:** street, corp, Ironclad, Rival, and Chrome Raider NPCs now fight like The Reach — autoshotguns, hand cannons, carbines, smartguns, sawed-offs, holdouts, grenades, stims, kill codes, and decks instead of zweihanders, halberds, handaxes, and crossbows. Most gun signatures are now Melee 1 or Ranged 5 (Draw Steel damage unchanged); a few chrome melee exceptions remain (cyberarms, monowire, mono-knife). Beasts, undead, and Signal Talkers keep natural attacks. *Pending Foundry verification.*
- `0.1.52` — **Bestiary originals from The Ossian Reach Handbook (wave 2a):** 20 new Ghostwire Bestiary NPCs — Colors Boss, Chrome Bruiser, Street Doc, Wrench Rigger, The Warlord, and Razorline Prime on the streets; Response Lieutenant, Corp Netrunner, Contract Enforcer, and Ironclad Warden for corp jobs; a new **Reach Critters** folder (Chrome-Rat, Tunnel-Bat, Scrap-Hound, Sink-Crawler, Gutter-Serpent); Canopy-Stalker and Reach Behemoth past the wall; and Watchdog, Scrambler, and Black ICE for Wired fights. Each is built on a Draw Steel stat block, fights with Reach gear, and has a Director note mapping its Handbook threat to Draw Steel level and echelon. *Pending Foundry verification.*
- `0.1.53` — **Named Reach bosses:** five campaign fixtures join the Ghostwire Bestiary — **Mama Cassavir, “the Switchboard”** (the Flats fixer who fights through her people), **Warden Krael, “the Landlord”** (a corrupt Grid warden with a badge, a sidearm, and a paper trail), **The Ferryman** (tollkeeper of the descent to the Sinks), **The Choirmother** (a Flats cult-mother whose Director notes explain her Early / Mid / Late fall), and **Ranger-Captain Cael Marrow, “the Gate”** (Cinderhold’s ranger protector). Each has a full Director biography — stratum, hook, what they hold, who they answer to, how to use them — plus negotiation values. *Pending Foundry verification.*
- `0.1.54` — **Run Generator (Directors):** a new **Run Generator** button in the Token controls (Directors only) rolls a Reach run in one click. Dial hero level, echelon, run type (Extraction, Data Steal, Sabotage, Protection, Wetwork, Courier, Recon, Exorcism, and a Wilds Survey stub), stratum from the Crown to the Wastes, heat, and Wired intensity, then **Generate**: a patron and pitch, objective and clocks, a ¥ pay band from the Economy chapter, 3–6 beats, opposition and escalation linked to the Ghostwire Bestiary at the right level, the ICE ladder for Wired runs, support contacts, and suggested scenes. Keep the seed to reproduce a run or **Reroll** for a new one, and **Create Journal** saves it to a Ghostwire Runs journal. Tables: `scripts/data/runs/`. Notes: `docs/directors/run-generator.md`. *Pending Foundry verification.*

## Origins pack layout (Ghostwire Ancestries)

The pack id and path are `origins`; Foundry shows it as **Ghostwire Ancestries** (`GHOSTWIRE.COMPENDIUM.origins`). Keep the id so UUIDs stay stable.

Each People gets one compendium Folder named after it (Pure Human, Corran, Elvani, …). The People's ancestry and all of its traits and abilities go inside that folder, never at the pack root. The source mirrors this: `src/packs/origins/<people>/` holds a `_folder.json` (the Folder document) plus that People's items, and each item's `folder` is set to that Folder's `_id`. The build fails if an item's `folder` doesn't match its directory.

## Skills (v1)

Rules: `docs/masters/GHOSTWIRE_SKILLS_MASTER.md`. Skills use the five Ghostwire characteristics (Physique, Reflex, Logic, Instinct, Persona); the social skill is **Insight**.

- **How:** `scripts/skills.mjs` replaces `ds.CONFIG.skills.groups` and `ds.CONFIG.skills.list` during `init` (Draw Steel localizes them afterwards), so the hero sheet, skill pickers, and test rolls show only Ghostwire skills, grouped. Keys are camelCase (`firearms`, `securitySystems`, `matrixTheory`, …); effects use `system.skills.modifiers.<key>.edges`.
- **Draw Steel grants:** skill advancements that name Draw Steel skills or groups — stock Backgrounds, Professions, and classes — are translated when the picker opens. A Draw Steel skill becomes its Ghostwire skill below; a Draw Steel group becomes the Ghostwire skills its members map to (for example *interpersonal* offers Command, Deception, Insight, Intimidation, Negotiation, Performance, Persuasion, and Survival from Handle Animals). If two Draw Steel skills map to the same Ghostwire skill, the grant offers it once.
- **Module content updated to Ghostwire keys:** Cyber-Eyes and Cyber-Ears (Perception), Running Gear and Muscle / Bone Lacing (Athletics), Skillwires (Repair), Vocal Modulator (Deception), Synth-Weave Coat (Stealth), Changer Forms (Stealth, Perception, Intimidation), Auxiliary Limbs — Athletics (Athletics, Acrobatics), Corp Glamor (Persuasion), Perseverance (Athletics), Aberrant Rapport (social group). Chrome edges on the same skill don’t stack.
- **Existing heroes:** skills they already picked under Draw Steel keys no longer display. Re-pick them (or re-run the skill advancement) on heroes made before this version.

| Group | Skills |
|---|---|
| Action | Athletics, Brawl, Melee, Firearms, Heavy Weapons, Stealth, Acrobatics, Perception, Survival |
| Technical | Hacking, Electronics, Engineering, Repair, Cybertech, Medicine, Demolitions, Security Systems |
| Knowledge | Streetwise, Corporate, History, Occult, Religion, Matrix Theory, Medicine Lore, Xenology |
| Social | Negotiation, Persuasion, Deception, Intimidation, Command, Insight, Performance, Contacts |
| Vehicle & Drone | Driving, Piloting, Rigging, Gunnery, Navigation |
| Magic & Supernatural | Spellcraft, Rituals, Warding, Resonance, Corruption, Summoning |

**Draw Steel → Ghostwire skill map** (used to translate stock grants)

| Ghostwire skill | Draw Steel skills |
|---|---|
| Athletics | Climb, Endurance, Jump, Lift, Swim |
| Stealth | Conceal Object, Hide, Pick Pocket, Sneak |
| Acrobatics | Gymnastics, Escape Artist |
| Perception | Alertness, Eavesdrop, Search |
| Survival | Cooking, Handle Animals, Track, Nature |
| Electronics | Jewelry |
| Engineering | Architecture, Carpentry |
| Repair | Blacksmithing, Fletching, Mechanics, Tailoring |
| Medicine | Heal |
| Demolitions | Sabotage |
| Security Systems | Pick Lock |
| Streetwise | Criminal Underworld, Rumors |
| Corporate | Society |
| History | Culture, History |
| Occult | Magic, Monsters |
| Religion | Religion |
| Matrix Theory | Timescape |
| Medicine Lore | Alchemy |
| Negotiation | Gamble |
| Persuasion | Flirt, Persuade |
| Deception | Forgery, Lie, Disguise |
| Intimidation | Interrogate, Intimidate |
| Command | Lead, Strategy |
| Insight | Empathize, Read Person |
| Performance | Brag, Music, Perform |
| Driving | Drive, Ride |
| Navigation | Navigate |
| Resonance | Psionics |

**Test click-path**

1. Create a new hero → **Stats** tab → switch to **Edit** mode → open the skills selector: it lists only Ghostwire skills in six groups (Firearms, Hacking, Streetwise, Insight, …) — no Alchemy, Blacksmithing, or other crafting skills.
2. Drag **Mutant** from **Ghostwire Ancestries**: Aberrant Rapport asks for one **Social** skill (Insight, Persuasion, …).
3. Add a stock Draw Steel Background (culture) or Profession (career) with a skill choice: the picker offers Ghostwire skills.
4. Drag **Cyber-Eyes** from **Ghostwire Chrome**: roll a Perception test — it has an edge.

## Backgrounds & Professions (v1)

Rules: `docs/rulebook/13-backgrounds-professions.md`. Draw Steel’s *culture* and *career* item types are labeled **Background** and **Profession** on the sheet.

- **Packs:** **Ghostwire Backgrounds** (`backgrounds`, `culture` items) and **Ghostwire Professions** (`professions`, `career` items). Each item has two skill advancements, both using Ghostwire skill keys: a fixed skill (`chooseN` empty, one choice) and a pick (`chooseN` 1 for Backgrounds, 2 for Professions, from four choices). No languages or perks. Professions set Renown, ¥, and project points to 0.
- **No duplicate picks:** `scripts/skills.mjs` filters a skill picker while it’s open. It leaves out skills the hero already has (sheet skills from any source) and skills chosen elsewhere in the same advancement run, but keeps the advancement’s own current picks when you reconfigure. If fewer than *choose N + 1* options remain, it pads the list with unowned skills from the same Ghostwire groups (then any group), so it’s always a real choice. The filter only applies while the picker is open — Draw Steel’s own data preparation sees the unfiltered list, so saved picks stay valid.
- **Fixed skills don’t collide:** Background fixed skills, Profession fixed skills, and Operator Origin skills (Command, Demolitions, Intimidation) are disjoint, so a Background + Profession never wastes a fixed grant. A fixed skill already granted by some other source (for example an ancestry pick made first) is not rerouted automatically; pick a replacement on the sheet.
- **Sheet buttons:** `scripts/module.mjs` points the hero sheet’s **+ Add** action at the Ghostwire packs for Ancestry (Ghostwire Ancestries), Background, and Profession. Class still opens the Draw Steel classes compendium.

| Background | Fixed | Choose 1 |
|---|---|---|
| Undercity Barrens | Stealth | Streetwise, Survival, Perception, Athletics |
| Corp Arcology | Corporate | Persuasion, Negotiation, Insight, Security Systems |
| Sprawl District | Streetwise | Negotiation, Contacts, Perception, Brawl |
| Transit Hub / Freeport | Navigation | Contacts, Driving, Piloting, Negotiation |
| Outland Wastes | Survival | Athletics, Driving, Perception, Firearms |
| Academic Spire | History | Occult, Matrix Theory, Medicine Lore, Xenology |
| Faith District | Religion | Insight, Occult, Warding, Medicine |
| Industrial Deep | Engineering | Repair, Athletics, Demolitions, Electronics |

| Profession | Fixed | Choose 2 |
|---|---|---|
| Wage-Slave | Negotiation | Corporate, Persuasion, Insight, Electronics |
| Street Doc | Medicine | Cybertech, Insight, Streetwise, Medicine Lore |
| Fixer | Contacts | Negotiation, Streetwise, Insight, Deception |
| Courier | Driving | Stealth, Navigation, Athletics, Streetwise |
| Gang Soldier | Firearms | Intimidation, Streetwise, Brawl, Melee |
| Security Guard | Perception | Firearms, Intimidation, Security Systems, Brawl |
| Smuggler | Security Systems | Deception, Contacts, Driving, Stealth |
| Rig-Tech | Rigging | Electronics, Repair, Driving, Piloting |
| Deck Jockey | Hacking | Matrix Theory, Electronics, Security Systems, Streetwise |
| Entertainer | Performance | Persuasion, Deception, Insight, Contacts |
| Beat Reporter | Perception | Corporate, Contacts, Insight, Electronics |
| Hauler / Labor | Athletics | Driving, Repair, Brawl, Engineering |
| Merc Recruit | Firearms | Athletics, Survival, Melee, Heavy Weapons |
| Acolyte | Occult | Religion, Insight, Warding, Rituals |
| Chop-Doc Assistant | Cybertech | Medicine, Streetwise, Demolitions, Electronics |

**Test click-path**

1. Create a new hero. Click **+ Add Background** (opens **Ghostwire Backgrounds**) and drag **Sprawl District** onto the hero. The advancement window shows the fixed **Streetwise** and a **Background Skill (choose 1)** picker: Negotiation, Contacts, Perception, Brawl. Pick **Negotiation**.
2. Click **+ Add Profession** and drag **Fixer**. Its fixed skill is **Contacts**. The **Profession Skills (choose 2)** picker would normally offer Negotiation, Streetwise, Insight, Deception — Negotiation and Streetwise are already yours, so it shows Insight and Deception plus one unowned skill from the same groups (Corporate) to keep it a real choice. Pick two.
3. **Stats** tab → Skills lists Streetwise, Negotiation, Contacts, and your two new picks — five different Ghostwire skills, no duplicates, no Draw Steel skills.

## Classes (v1)

Rules: `docs/rulebook/01-operator.md` (authoritative), `docs/masters/GHOSTWIRE_OPERATOR_DEVELOPMENT_MASTER.md`. The **Ghostwire Classes** compendium (pack id `classes`) holds folder **Operator** › **Abilities** / **Origins** › Corp-Milspec, Merc, Street-vet.

### Operator

- **Structure:** the Operator is the Draw Steel **Fury** tree cloned and renamed (Ferocity → Adrenaline, Berserker → Corp-Milspec, Reaver → Merc, Stormwight → Street-vet). Every 7/9/11 ability, Origin doctrine ability, and feature keeps its Draw Steel power rolls and effects; names, text, and feature levels come from the chapter. Class: Physique + Reflex, Stamina 21 (+9), Recoveries 10, heroic resource **Adrenaline** (1d3 per turn), epic resource **Combat Legend**.
- **No magic:** the Operator casts nothing, so heroic and doctrine abilities that had Draw Steel’s Magic keyword use **Tech** instead (a Cyborg Operator can use all of them under Arcane Severance).
- **Adrenaline costs:** every heroic ability’s cost is its Draw Steel `resource` (base band 1–5, bands 7/9/11, Origin doctrine abilities 5/9/11); signatures and the Origin triggered abilities cost nothing. Draw Steel’s own use dialog doesn’t stop you spending Adrenaline you don’t have, so `scripts/module.mjs` refuses to use a heroic ability **in combat** when the hero’s current heroic resource is below its cost (warning, no dialog). Outside combat abilities stay usable without spending, per the chapter. The dialog still lets you type a different spend amount.
- **Ghostwire-original:** signatures **Controlled Pair** (ranged 10, 3/6/9 + Reflex) and **Suppressing Fire** (3 cube within 10, 1/2/3), both granted at 1st level; base band **Breach & Clear** (1), **Trigger Cadence** (2), **Hold the Line** (2), **Overwatch** (3), **Adrenaline Dump** (5), choose one at 1st level. All roll with **Reflex**; base-band damage is flat as printed in the chapter.
- **Skills:** 1st level — choose two from the **Action** or **Vehicle & Drone** groups; 4th, 7th, and 10th — any Ghostwire skill. Origin skills are fixed: Corp-Milspec **Command**, Merc **Demolitions**, Street-vet **Intimidation**. Pickers leave out skills the hero already has (same rule as Backgrounds and Professions).
- **Origins:** each picks from the 21 Ghostwire Kits. **Merc** picks **two** and gets **Field Arsenal**: Draw Steel uses the better Stamina, speed, stability, disengage, and distance of the two Kits and the damage bonus of the **preferred Kit** — on the sheet’s **Equipment** tab, right-click a Kit → **Make Preferred Kit** (a star marks it). **Street-vet** picks a **Loadout Damage Type** (Cryo = cold, Toxin = corruption, Incendiary = fire, Shock = lightning) used by its abilities; at 4th and 10th level, enable the matching immunity effect on Combat Form / Warzone Incarnate.
- **Level table — follows the chapter.** Draw Steel advancements are independent per-level grants with no dependencies between them, so the Fury slots were moved to match `01-operator.md`: **4th** — Damaging Adrenaline, Growing Adrenaline I, Combat Attunement, Precision Strike, Veteran of the Sprawl, Breach Point, Combat Form, Greater Adrenaline (Fury had the last four at 6th/7th); **7th** — Growing Adrenaline II; **Corp-Milspec Anchored Stance** — 6th (Fury: 3rd). Adrenaline income is unchanged in shape: Greater Adrenaline (1d3 + 1 per turn) and Damaging Adrenaline both arrive at 4th, Peak Adrenaline at 10th. This front-loads 4th level compared to Draw Steel’s Fury.
- **Omitted:** Fury’s Mighty Leap and fixed Nature skill (not in the chapter). Street-vet’s Growing Adrenaline table is cloned from the Merc’s, since the Stormwight’s lived in its animal-form kits. Lock Down! still applies Draw Steel’s **Petrified** condition (called “locked down” in its text). Perks are still Draw Steel perks.

**Create an Operator hero**

1. Create a hero. On the sheet, **+ Add Ancestry** → drag **Pure Human** (or any People) and finish its picks.
2. Drag **Operator** from **Ghostwire Classes** › Operator. In the advancement window: choose two **Action / Vehicle & Drone** skills, an **Operator Origin**, and one **Heroic Ability (1–5 Adrenaline)**. Adrenaline, Controlled Pair, and Suppressing Fire are granted automatically.
3. Configure the Origin: its skill is fixed (Command, Demolitions, or Intimidation); pick the Kit (Merc: two Kits; Street-vet: also the Loadout Damage Type).
4. **+ Add Background** → drag one (for example **Sprawl District**); take the fixed skill and choose 1. **+ Add Profession** → drag one (for example **Gang Soldier**); choose 2. Skills you already have — including your Origin skill — don’t appear in these pickers. *(Add the class before Background/Profession: Origin skills are fixed grants, so a Background/Profession pick made first could duplicate them.)*
5. **Merc only:** Equipment tab → right-click the Kit whose damage you want → **Make Preferred Kit**.
6. Drag qualifying gear from **Ghostwire Gear** (for example the **Longshot** marksman rifle for the Longshot Kit, or the **Sleeve-Gun** + **Armored Jacket** for Ghost).
7. Check: Stats shows **Adrenaline**; Skills are all Ghostwire skills with no duplicates; Abilities lists Controlled Pair, Suppressing Fire, the chosen heroic ability, the Kit signature ability, and the Origin’s triggered ability (Kinetic Redirect, Wired Reflexes, or Overclock Nerves); Features include Field Arsenal (Merc) or your Loadout (Street-vet).

### Scout

Rules: `docs/rulebook/02-scout.md` (authoritative), `docs/masters/GHOSTWIRE_SCOUT_DEVELOPMENT_MASTER.md`. Folder **Scout** › **Abilities** / **Origins** › Hunter, Ghost, Face-in-crowd.

- **Structure:** the Draw Steel **Shadow** tree cloned and renamed (Insight → **Advantage**, Umbral Form → **Ghost Suite**, Gloom Squad → **Ghost Squad**, Keen Insight → **Sharper Eye**, Surge of Insight → **Surge of Advantage**). Class: Reflex + Logic, Stamina 18 (+6), Recoveries 8, 1d3 Advantage per turn, epic resource **Subterfuge**. Heroic abilities keep Draw Steel power rolls; names and text come from the chapter, and strikes roll with **Reflex**.
- **Subclasses:** **Hunter** is Ghostwire-original (Quarry, Ranged Reflex, Steady the Scope, Called Shots, Overwatch Lane, Ghost Round, One Shot One Kill, Vantage Trap). **Ghost** reskins Black Ash (Blink Rig: “Blink” = reposition, not teleport). **Face-in-crowd** reskins Harlequin Mask (Mask Rig keeps I’m No Threat’s edge and Disengage effects). Subclass skills: Survival, Demolitions (sabotage), Deception (lie).
- **No magic:** no Scout ability has Magic. Former Magic abilities use **Chrome** (implant-driven) or **Optics** (emitter/holo-driven), registered in `scripts/module.mjs`. Corruption damage became untyped damage called thermal-optic or thermal-kinetic in the text; Ghost Suite’s thermal-optic immunity is rules text.
- **Advantage costs:** heroic abilities carry their cost in `resource`, so the existing in-combat check blocks a heroic ability when current Advantage is below its cost (Hesitation Is Weakness and Diversion cost 1). The Scout’s “1 fewer Advantage with an edge” discount isn’t automated; the check uses the full cost.
- **Surges:** feature text gives the Ghostwire value (+2/+3/+4/+5 damage per surge by echelon). Gaining surges uses Draw Steel’s `[[/surge]]` links; how much damage a spent surge adds is still Draw Steel’s own calculation.
- **Skills and Kit:** Stealth is free; then choose five from Action, Social, Technical, or Knowledge (owned skills are left out). 4th/7th/10th: any skill. Kit pool is the 11 light Kits (no or light armor, no heavy weapon, not Hexshot/Spellblade/Sanctified); Quick Build Ghost.
- **Ghost Suite** is an effect you apply from the feature (climb, edge on strikes against you, bane on Persona tests; ends at combat end). Quarry has a **Marked (Quarry)** effect to apply to the target.

## Perks (v1)

Rules: `docs/rulebook/17-perks.md`. The **Ghostwire Perks** compendium (pack id `perks`) holds 47 perks in six type folders (Crafting 6, Exploration 10, Interpersonal 10, Intrigue 6, Lore 8, Supernatural 7) and a **Perk Abilities** folder.

- **Structure:** each perk is cloned from its Draw Steel perk with the same `perkType` and mechanics; names and text are Ghostwire (skill groups read as Technical / Action / Social / Knowledge, outcomes as low / middle / high results). Polyglot keeps Linguist’s two-language grant; Echo Reader keeps Thingspeaker’s bane effect; Veil Familiar still links the Draw Steel familiar stat block.
- **Perk abilities:** Veil Trick (Magic), Ghost Touch and Wire Whisper (Psionic, read as the Signal) are module copies; their perks grant the module copies, not the Draw Steel abilities.
- **Perk grants:** Draw Steel perk advancements have an empty pool and only accept a dropped perk. `scripts/module.mjs` fills the choice list with every Ghostwire perk whose type the grant allows (all six when the grant says any perk), refuses Draw Steel perks dropped from `draw-steel.character-options`, and removes those perks from `ds.registry.perk`. Ghostwire perks can still be dropped in. The six perk types are registered with Ghostwire labels, because Draw Steel derived them from the skill groups Ghostwire replaced.
- **Not hidden:** the Draw Steel perks still appear if you open the Draw Steel character-options compendium directly (that pack also holds kits and titles).

## Kits (v1)

Rules: `docs/rulebook/10-kits.md`. The **Ghostwire Kits** compendium (pack id `kits`) holds all 21 Kits in five role folders. Each Kit folder also holds that Kit’s signature ability.

- **Structure:** each Kit is a Draw Steel `kit` item cloned from its source kit. It has the same equipment block and bonus fields (Stamina per echelon, speed, stability, disengage, melee/ranged damage by result, distance) and a signature-ability grant pointing at the Ghostwire copy.
- **Numbers are provisional.** They are the Draw Steel kit benchmarks unchanged, pending the Kits numeric pass. Signature abilities keep their Draw Steel numbers; names, story, and effect text are reskinned (Might/Agility read as Physique/Reflex).
- **Doctrine never costs ¥**, so Kits have no price. Every description repeats the ownership rule: no qualifying gear owned means no Kit bonuses (not automated; the Director and player apply it).
- **Keywords:** Duelist’s Gravity Cut uses **Tech** instead of Psionic. Hexshot, Spellblade, and Sanctified signatures keep **Magic** (Cyborgs can’t use them under Arcane Severance).

| Kit | Folder | Draw Steel source | Gear category | Bonuses (damage low/mid/high) | Signature |
|---|---|---|---|---|---|
| Longshot | Ranged specialists | Sniper | Precision rifle; no armor | Speed +1, Disengage +1, Ranged +0/+0/+4, Ranged distance +10 | Held Breath |
| Saturation | Ranged specialists | Rapid-Fire | SMG or carbine; light armor | Stamina +3, Speed +1, Disengage +1, Ranged +2/+2/+2, Ranged distance +7 | Double Tap |
| Ghost | Ranged specialists | Cloak and Dagger | One or two silenced light weapons; light armor | Stamina +3, Speed +2, Disengage +1, Melee +1/+1/+1, Ranged +1/+1/+1, Ranged distance +5 | Ghost Out |
| Hexshot | Ranged specialists | Arcane Archer | Bow, crossbow, or dartgun; no armor | Speed +1, Disengage +1, Ranged +2/+2/+2, Ranged distance +10 | Hex Round |
| Gunslinger | Gun/blade finesse | Swashbuckler | Medium sidearm or blade; light armor | Stamina +3, Speed +3, Disengage +1, Melee +2/+2/+2 | Quickdraw Shuffle |
| Streetsweeper | Gun/blade finesse | Ranger | Shotgun or carbine plus a medium weapon; medium armor | Stamina +6, Speed +1, Disengage +1, Melee +1/+1/+1, Ranged +1/+1/+1, Ranged distance +5 | Kneecap Shot |
| Raider | Gun/blade finesse | Raider | Light weapon plus a shield; light armor | Stamina +6, Speed +1, Disengage +1, Melee +1/+1/+1, Ranged +1/+1/+1, Ranged distance +5 | Shock and Awe |
| Juggernaut | Heavy / tank | Mountain | Heavy weapon; heavy armor | Stamina +9, Stability +2, Melee +0/+0/+4 | Payback |
| Warframe | Heavy / tank | Shining Armor | Medium weapon plus a shield; heavy armor | Stamina +12, Stability +1, Melee +2/+2/+2 | Hold the Line |
| Breacher | Heavy / tank | Sword and Board | Medium weapon plus a ballistic shield; medium armor | Stamina +9, Stability +1, Disengage +1, Melee +2/+2/+2 | Breach and Bash |
| Bulldozer | Heavy / tank | Panther | Heavy weapon; no armor | Stamina +6, Speed +1, Stability +1, Melee +0/+0/+4 | Freight Train |
| Brawler | Melee / martial | Pugilist | Unarmed or cyberlimb strikes; no armor | Stamina +6, Speed +2, Stability +1, Melee +1/+1/+1 | Stagger Combo |
| Mantis | Melee / martial | Martial Artist | Unarmed or cyberlimb strikes; no armor | Stamina +3, Speed +3, Disengage +1, Melee +2/+2/+2 | Mantis Feint |
| Chromeblade | Melee / martial | Dual Wielder | A light plus a medium melee weapon; medium armor | Stamina +6, Speed +2, Disengage +1, Melee +2/+2/+2 | Twin Cut |
| Reach | Melee / martial | Guisarmier | Polearm or long cyber-weapon; medium armor | Stamina +6, Stability +1, Melee +2/+2/+2, Melee distance +1 | Haft and Blade |
| Monowhip | Melee / martial | Whirlwind | Whip, monofilament, or chain; no armor | Speed +3, Disengage +1, Melee +1/+1/+1, Melee distance +1 | Monofilament Lash |
| Snarehunter | Melee / martial | Retiarius | Net or ensnaring gear plus a polearm; light armor | Stamina +3, Speed +1, Disengage +1, Melee +2/+2/+2, Melee distance +1 | Tangle and Stab |
| Staff Adept | Melee / martial | Stick and Robe | Staff or polearm; light armor | Stamina +3, Speed +2, Disengage +1, Melee +1/+1/+1, Melee distance +1 | Staff Redirect |
| Duelist | Melee / martial | Battlemind | Medium melee weapon; light armor | Stamina +3, Speed +2, Stability +1, Melee +2/+2/+2 | Gravity Cut |
| Spellblade | Magic-tech | Spellsword | Medium melee weapon plus a shield; light armor | Stamina +6, Speed +1, Stability +1, Melee +2/+2/+2 | Arc Blade |
| Sanctified | Magic-tech | Warrior Priest | Light weapon; heavy armor | Stamina +9, Speed +1, Stability +1, Melee +1/+1/+1 | Veil Brand |

**Test click-path**

1. Compendium sidebar → **Ghostwire Kits** → five folders (Ranged specialists, Gun/blade finesse, Heavy / tank, Melee / martial, Magic-tech) holding 21 Kits.
2. Open **Longshot**: the description shows gear, ownership rule, provisional bonuses, and a link to Held Breath; the details show Firearm / Bow and no armor.
3. Drag **Longshot** onto a hero (with a class): the Kit appears on the sheet, **Held Breath** is added to Abilities, speed is +1, and Held Breath and the ranged free strike show +10 distance and +4 damage on the high result.
4. Swap to **Brawler**: Stamina max rises by 6 (echelon 1), stability +1, speed +2, melee free strike +1/+1/+1, and **Stagger Combo** is added.
5. Swap to **Juggernaut**: Stamina +9, stability +2, melee free strike +0/+0/+4, and **Payback** is added.

## Gear catalog (v2)

Source of record: `docs/masters/GHOSTWIRE_GEAR_MASTER.md` — every table row is a Foundry item; prices, slots, profiles, and tags are copied from the master. Rules: `docs/rulebook/10-kits.md` (ownership rule), `docs/rulebook/11-economy.md`, `docs/rulebook/12-chrome.md`.

| Compendium | Pack id | Items | Master sections | Folders |
|---|---|---|---|---|
| **Ghostwire Gear** | `gear` | 107 | 1A–1G, 2A–2E, 3A–3F | General & Lifestyle › Comms, Sensors, Break-in, Survival, Medical, Ammunition & Thrown, Lifestyle · Armor › Light, Medium, Heavy, Sealed & Hardened, Shields · Weapons › Light Firearms, Longarms, Heavy, Melee & Blades, Thrown & Grenades, Bows/Crossbows/Exotic |
| **Ghostwire Mods** | `mods` | 37 | 3G, 5F (Armor + Weaponry ladders) + B20 armor/gadget mods | Weapon Mods · Vehicle & Drone Mods · Armor & Shield Mods · Gadget Mods |
| **Ghostwire Matrix** | `matrix` | 37 | 4A–4F + B20 RCC autosofts | Cyberdecks · Programs · Payloads · RCCs · RCC Autosofts · Cyberjacks & Interfaces · Matrix-Support |
| **Ghostwire Vehicles & Drones** | `vehicles` | 68 | 5A–5E + `15-drones.md` / `16-vehicles.md` | Ground · Air · Water & Submersible · Space · Drones |
| **Ghostwire Foci** | `foci` | 41 | 6A–6F | Elementalist · Street-Priest · Technomancer · Shared · Material Components · Reagent Packages |
| **Ghostwire Bestiary** (Actors, Director-only) | `bestiary` | 35 | `GHOSTWIRE_BESTIARY.md` wave 1 | Reach Streets · Corp & Security · Wire & Machine · Veil & Undead · Wilds & Jungles · Rivals |
| **Ghostwire Summons & Machines** (Actors) | `summons` | 31 | Support Entities §3.1–3.4, Machine Bands | Sprites (Technomancer) · Elementals & Companions (Elementalist) · Pact Spirits (Street Priest) · Drones & Vehicles (scale-band templates for Deploy) · Wired Nodes (empty, later phase) |

- **Grade = echelon + Availability.** Ghostwire uses Draw Steel levels 1–10 and echelons 1–4 only; the master’s old T5–T1 column maps to **Echelon 1 Street** (T5), **Echelon 1 Professional** (T4), **Echelon 2 Restricted** (T3), **Echelon 3 Military** (T2), **Echelon 4 Prototype** (T1). Items store it in `system.echelon` and the flag; descriptions say “Echelon N · Availability: Street/…”. A few row texts that named the old ladder were reworded (for example “Professional-grade (or lower) locks”, “a middle-result Wired roll”). Weapon mods (3G) have no grade in the master, so they show “Echelon —”.
- **Items:** Draw Steel `treasure`. Weapons `kind: weapon` (keywords: `light`/`medium`/`heavy` from the band — Anti-veh counts as heavy — plus `bow` for firearms, bows, and launchers, `ensnaring` for the Net-Gun, `whip` for the Monowhip); armor and shields `kind: armor` (keyword = armor class or `shield`); everything else `kind: other`, `category: trinket`.
- **Flags:** `flags.draw-steel-ghostwire.gear | mod | matrix | vehicle | focus` = `{ echelon, availability, price, modSlots, tags, consumable? }`, plus weapons `damage, damageType, range, weaponBand`; armor `armorClass` (`light|medium|heavy|shield|sealed`, sealed also `baseClass`), `staminaByEchelon`, `immunity`; mods `host, slotCost, craftSkill` (vehicle/drone armor kits also `staminaBonus`, `exclusiveKit`); matrix `role`; vehicles `domain, scale, drone`; foci `tradition` (components also `drainTrade`). `priceText`/`priceNote` cover “per §F3”, “(holds value)”, and “80,000+”. The item sheet shows “Echelon · Availability · ¥ · Mod slots” under the name.
- **Weapons:** the description lists the master’s **Weapon Base** damage, type, band, and range. No damage effects — Kit doctrine still supplies Draw Steel damage bands.
- **Armor as Stamina:** each armor lists its Stamina by hero echelon (Echelon 1 uses the street numbers for Street items and the professional numbers for everything else; Echelons 2–4 use the old T3/T2/T1 columns) and carries four **disabled** “No Kit — Echelon N” effects. With a Kit, the Kit’s Stamina already is the armor’s Stamina — leave them off. Shields stack with any armor. Sealed & Hardened armor also carries a disabled **typed immunity** effect (Flashweave fire 2, Faraday Suit lightning 2, Sealed Armor cold 2, HE Combat Suit fire/cold/poison 2 — toxin maps to poison).
- **Mods and §Craft:** §Craft is the downtime Project procedure, never a skill. Every mod, program, and autosoft says what it fits, its slot cost, and its Project skill: weapon/armor/shield/vehicle physical mods **Repair**; gadget and smart hardware **Electronics**; programs and payloads **Hacking**; autosofts **Hacking or Rigging**; chrome-adjacent **Cybertech**. Installing/swapping/removing is downtime; toggling an installed mod or loaded program is a field action. Mods are inventory items; there is no attach UI yet. Flags: `mod` = `{ …, host, hosts, slotCost, craftSkill: [skill keys] }`.
- **Slot integrity:** every item with mod slots has `modFamily` in its flag and lists the fitting mods (as links) in its description — weapons, vehicles & drones, armor, shields, gadgets (comms, sensors, mechanical/electronic break-in tools, survival kits, Wired interfaces and support gear), cyberdecks (programs + payloads), and RCCs (autosofts). 33 items with no mod family have 0 slots and say so: Field Surgery Kit, Designer Threads, Faraday Bag, and the 30 foci. Rules: `docs/rulebook/14-mods.md`.
- **Consumables:** anything tagged Consumable (plus matrix payloads, ritual components, and reagent packages) has `modSlots: 0`. This overrides two master rows that listed slots on a Consumable (Respirator Mask 1, Cutting Torch 3); the Burner is tagged “Consumable-ish” and keeps its 1 slot.
- **Implants:** the Cyber-Spur is a price listing that links the chrome **Implant Weapon (Spur)**, which spends Body Integrity.
- **Not automated:** buying (¥ not deducted), Availability gating, mod slot tracking, vehicle/drone stat blocks (deferred in the master), focus bonding.

**Test plan**

1. Foundry closed → `node tools/build-packs.mjs` (no errors).
2. Start Foundry, enable the module: the Compendium sidebar lists **Ghostwire Gear**, **Ghostwire Mods**, **Ghostwire Matrix**, **Ghostwire Vehicles & Drones**, **Ghostwire Foci**.
3. Spot-check: **Smartlink** (Mods › Weapon Mods) ¥500, slot cost 1, Wired/Smart; **Sleeve-Gun** (Gear › Weapons › Light Firearms) Echelon 1 · Street · ¥150; **Hardshell** (Gear › Armor › Heavy) four disabled No Kit effects; **Scrapdeck** (Matrix › Cyberdecks); **Fly** (Vehicles › Drones); **Spark-ring** (Foci › Elementalist).
4. Open a few folders — nested structure is readable.
5. Browser console has no errors.
6. Mods pack shows four folders: Weapon Mods, Vehicle & Drone Mods, Armor & Shield Mods, Gadget Mods. Matrix has an RCC Autosofts folder.
7. Open **Reader** (Matrix › Programs): its text says Hacking, not Craft. Open **Lockpick Set** (Gear › General › Break-in): its mod slot note links **Quiet Picks**. Open **Spark-ring**: it explains its 0 slots.

## Chrome & Body Integrity (v1)

Rules: `docs/rulebook/11-economy.md`, `docs/rulebook/12-chrome.md`, `docs/masters/GHOSTWIRE_CHROME_MASTER.md`.

- **Nuyen** is Draw Steel `system.hero.wealth`, relabeled in lang. It’s shown in the hero **Biography** tab; switch the sheet to **Edit** mode to type a value. Heroes created with Ghostwire enabled start at ¥5,000. Older heroes keep whatever they had (default 1), so set 5000 by hand for tests.
- **Body Integrity** is stored in `flags.draw-steel-ghostwire.integrity` (`value`/`max`; living default **20/20**, Cyborg **25/25**) and edited in the **Body Integrity** box at the top of the **Stats** tab. Cyborgs show value/max like everyone else.
- **Chrome** implants are `treasure` items in **Ghostwire Chrome**. Each carries `flags.draw-steel-ghostwire.chrome` (`grade`, `location`, `integrity`, `price`, `availability`, optional `grants`) and an always-on transferred effect. Draw Steel treasure has no price field, so ¥ shows in the description header and in a line under the name on the item sheet.
- **Install** = drag onto a hero (including Cyborgs). `scripts/module.mjs` blocks heroes without enough Integrity and full locations; then it spends the Integrity and adds any granted abilities (Implant Weapon → Spur Strike). **Delete** returns 75% of the Integrity (round down) and removes granted abilities.
- **Slot caps:** Head 3 · Eyes 1 · Ears 1 · Torso 3 · Arms 4 (2 per arm) · Legs 4 (2 per leg) · Nervous System 1.
- **Not automated yet:** buying (¥ isn’t deducted), Availability gating, the no-double-dip rule between different implants, surgery Projects, and Suppressed/Damaged states.

| Implant | Location | Integrity | Price | Availability | Automated |
|---|---|---|---|---|---|
| Datajack | Head | 1 | ¥500 | Street | effect marker |
| Vocal Modulator | Head | 1 | ¥800 | Professional | edge on Lie |
| Implant Weapon (Spur) | Arms | 1 | ¥1,000 | Restricted | grants Spur Strike |
| Internal Air / Filtration | Torso | 2 | ¥1,500 | Professional | poison immunity 2 |
| Cyber-Ears | Ears | 2 | ¥1,800 | Professional | edge on Eavesdrop |
| Cyber-Eyes | Eyes | 2 | ¥2,000 | Professional | edge on Alertness, Search |
| Running Gear | Legs | 3 | ¥2,500 | Professional | +1 speed, edge on Jump |
| Reaction Enhancer | Nervous | 3 | ¥3,500 | Restricted | +1 Disengage |
| Dermal Plating | Torso | 4 | ¥4,000 | Restricted | damage immunity 2 (all) |
| Skillwires / Encephalon | Head | 4 | ¥5,000 | Restricted | edge on Mechanics (edit key to swap) |
| Muscle / Bone Lacing | Torso | 5 | ¥6,000 | Restricted | +1 stability, edge on Lift |
| Cyberlimb (Arm) | Arms | 5 | ¥7,500 | Restricted | +1 melee weapon damage |
| Wired Reflexes | Nervous | 6 | ¥12,000 | Military | +1 speed, +2 Disengage |

**Prices are provisional** (Standard grade only). Grade variants are a later pass; rough rule for now:
- **Salvage / Used** ≈ 0.5× ¥ and 1.5× Body Integrity.
- **Soft / Bioware** ≈ 3× ¥ and 0.4× Body Integrity (round up).

Chrome **packages** (Roadrunner, Argus, Bulwark, …) are not in the compendium yet.

**Test click-path**

1. Create a new hero → **Biography** tab: Nuyen 5000.
2. **Stats** tab: **Body Integrity** Current 20 / Max 20.
3. Compendium sidebar → **Ghostwire Chrome** → open **Wired Reflexes**: under the name the sheet reads “Standard · Nervous System · Body Integrity 6 · ¥12,000 · Military”, and the description starts with the same header.
4. Drag **Cyber-Eyes** onto the hero: notice says 2 spent; Integrity 18/20; item in **Equipment**; effect in **Effects**; Alertness and Search tests roll with an edge.
5. Drag **Implant Weapon (Spur)**: Integrity 17/20 and **Spur Strike** appears in **Abilities**. Delete the implant: Spur Strike is removed (Integrity back to 17, since 75% of 1 rounds down to 0).
6. Drag **Reaction Enhancer** (Disengage 2 in Movement), then try **Wired Reflexes**: blocked because Nervous System is full.
7. Drag **Dermal Plating** (Stats → Immunities: All 2) and **Cyberlimb (Arm)** (melee free strike +1 damage on each tier). Keep adding until an implant costs more than the Integrity left: it’s blocked with a warning.
8. Delete Cyberlimb (Arm): 3 Integrity returns.
9. Hero with the **Cyborg** ancestry: Stats shows Body Integrity **25/25**; dragging chrome spends Integrity like a living hero. Arcane Severance still blocks Veil caster classes.

## Building packs

Pack sources live in `src/packs/<pack>/` (subfolders allowed). Names and descriptions are `GHOSTWIRE.*` lang keys, filled in from `lang/en.json` at build time. With Foundry closed, run `node tools/build-packs.mjs` to rebuild `packs/`. It uses the `classic-level` package bundled with Foundry; set `FOUNDRY_APP` if Foundry isn't installed in the default location.

Vehicle and drone token art (B101, **0.3.31**): 36 drone + 32 vehicle WebPs in `assets/tokens/drones/` and `assets/tokens/vehicles/`. Re-apply after replacing files with `node tools/apply-machine-token-art.mjs` (rebuilds `packs/vehicles`). **0.3.68:** Mule-Bot cargo plate is `mule-bot.{png,webp}` and is also the generic **Drone (Medium)** Actor portrait. **0.3.69:** Lane-Hopper and Star-Chopper plates under `assets/tokens/vehicles/`. **0.3.80:** Seal Cruiser, Writ VTOL, White Door, Crash Angel. Spike: `docs/spikes/B101-VEHICLE-DRONE-TOKEN-ART.md`.

Armor + weapon item art (B102, **0.3.32**): 22 armor/shield + 49 weapon WebPs in `assets/tokens/armor/` (includes shields) and `assets/tokens/weapons/`. Re-apply after replacing files with `node tools/apply-gear-token-art.mjs` (stamps Gear pack + pregen embeds; rebuilds `packs/gear` + `packs/pregens`). Spike: `docs/spikes/B102-ARMOR-WEAPON-ITEM-ART.md`.

Deadhead playtest (B104, **0.3.33** / map pack B106, **0.3.36** / cargo **0.3.40** / live-scene lock **0.3.41** / Director journal **0.3.42** / Wire ping + Technomancer Whiteout **0.3.45** / **Gear SKUs 0.3.52** / **Nox trash freighter + aerial recon 0.3.54** / **hangout Scene removed 0.3.55**): Director SoR `docs/directors/runs/deadhead/DEADHEAD-GOLD-LINE.md`. JournalEntry pack **Ghostwire Runs** (`src/packs/runs/` → `packs/runs`) with folder **Deadhead** (Director journal + Gold Line map notes + aerial recon handout). Generate the Director journal with `node tools/deadhead-to-journals.mjs`. Rebuild with `node tools/build-packs.mjs runs`. Gold Line Scene inject: `scripts/gold-line-scene.mjs` + `data/scenes/gold-line.json` (new worlds only; existing `goldLineScene` never rewritten). Beat 0 hangout Scene is **REMOVED permanently 0.3.55** — no inject, do not reintroduce. Canyon Scene is **SKIPPED** — narrate the approach. **Gear SKUs shipped 0.3.52** (Mama’s Deadhead Brief + ARG Courier Capsule). **Nox trash freighter shipped 0.3.54** (Vehicles → Air). **Aerial recon photo shipped 0.3.54** (Journal + Plot gear Item; not baked into the battlemap). Spikes: `docs/spikes/B104-DEADHEAD-FOUNDRY-RUN-PACK.md`, `docs/spikes/B106-GOLD-LINE-MAP-PACK.md`, `docs/spikes/B106-WIRED-CONSOLE-WIRE-PING.md`, `docs/spikes/B107-DEADHEAD-HANGOUT.md` (**KILLED**), `docs/spikes/B109-TECHNOMANCER-DECKLESS-PAYLOADS.md`.

Bestiary + L≤4 summon portraits (B103, **0.3.35**): 40 bestiary (humanoids + ICE) + 17 summon WebPs in `assets/tokens/bestiary/` and `assets/tokens/summons/`. Re-apply after replacing files with `node tools/apply-bestiary-portrait-art.mjs` (stamps Actor `img` + `prototypeToken.texture.src`; rebuilds the packs that changed). Spike: `docs/spikes/B103-BESTIARY-HUMANOID-PORTRAITS.md`.

## Rulebook

- [Stage 1 — Core Rulebook skeleton](docs/rulebook/00-STAGE1-skeleton.md) (awaiting review)
