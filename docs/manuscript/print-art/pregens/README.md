# Pregen plates (held)

`GHOSTWIRE_Pregen_Art_Bundle` copies land here. **Not** in the v1 `ART-PLACEMENT.yml` map — journals / pregen dossiers already ship portraits under `assets/pregens/`. Held so the copy script has a destination.
