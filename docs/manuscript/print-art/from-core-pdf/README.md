# Core Sourcebook extracts

Images lifted from *GHOSTWIRE — Core Sourcebook.pdf* (local, gitignored).

```text
pip install pymupdf
python tools/extract-core-pdf-art.py --pdf "…Core Sourcebook.pdf" --out docs/manuscript/print-art/from-core-pdf
```

Used only as **fallbacks** when a named cover / class / filler file is missing. Inject matches by filename tokens (`cover`, `operator`, `wired`, …). Do not commit the PDF or the extracts.
