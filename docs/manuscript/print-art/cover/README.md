# Cover / title plate

Put the book cover here as `cover.webp` (or `cover.png`). Core PDF extract named `*cover*` is a fallback.

Replaces the reserved hole on `docs/manuscript/00-front/title-page.md`. Credit: Ghostwire AI.
